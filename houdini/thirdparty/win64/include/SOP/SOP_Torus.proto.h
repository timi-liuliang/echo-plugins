/* Automagically Generated by generate_proto.py
 * Do not Edit
 */
#pragma once

#include <SOP/SOP_NodeVerb.h>
#include <OP/OP_Utils.h>
#include <PRM/PRM_Parm.h>
#include <UT/UT_IStream.h>
#include <UT/UT_NTStreamUtil.h>
#include <UT/UT_Ramp.h>
#include <UT/UT_SharedPtr.h>
#include <UT/UT_StringHolder.h>
#include <UT/UT_StringStream.h>
#include <UT/UT_VectorTypes.h>
#include <SYS/SYS_Types.h>

using namespace UT::Literal;

class DEP_MicroNode;
namespace SOP_TorusEnums
{
    enum class Type
    {
        POLY = 0,
        MESH,
        NURBS,
        BEZIER,
        POLYSOUP
    };
    enum class Surftype
    {
        ROWS = 0,
        COLS,
        ROWCOL,
        TRIANGLES,
        QUADS,
        ALTTRIANGLES,
        REVTRIANGLES
    };
    enum class Orient
    {
        X = 0,
        Y,
        Z
    };
}


class SOP_API SOP_TorusParms  : public SOP_NodeParms
{
public:
    static int version() { return 1; }

    SOP_TorusParms()
    {
        myType = 0;
        mySurftype = 4;
        myOrient = 1;
        myRad = UT_Vector2D(1,0.5);
        myT = UT_Vector3D(0,0,0);
        myR = UT_Vector3D(0,0,0);
        myScale = 1;
        myImperfect = true;
        myOrderu = 4;
        myOrderv = 4;
        myCloseu = true;
        myClosev = true;
        myCapu = false;
        myCapv = false;
        myRows = 12;
        myCols = 24;
        myAngleu = UT_Vector2D(0,360);
        myAnglev = UT_Vector2D(0,360);

    }

    explicit SOP_TorusParms(const SOP_TorusParms &) = default;

    virtual ~SOP_TorusParms() {}

    bool operator==(const SOP_TorusParms &src) const
    {
        if (myType != src.myType) return false;
        if (mySurftype != src.mySurftype) return false;
        if (myOrient != src.myOrient) return false;
        if (myRad != src.myRad) return false;
        if (myT != src.myT) return false;
        if (myR != src.myR) return false;
        if (myScale != src.myScale) return false;
        if (myImperfect != src.myImperfect) return false;
        if (myOrderu != src.myOrderu) return false;
        if (myOrderv != src.myOrderv) return false;
        if (myCloseu != src.myCloseu) return false;
        if (myClosev != src.myClosev) return false;
        if (myCapu != src.myCapu) return false;
        if (myCapv != src.myCapv) return false;
        if (myRows != src.myRows) return false;
        if (myCols != src.myCols) return false;
        if (myAngleu != src.myAngleu) return false;
        if (myAnglev != src.myAnglev) return false;

        return true;
    }
    bool operator!=(const SOP_TorusParms &src) const
    {
        return !operator==(src);
    }
    using Type = SOP_TorusEnums::Type;
    using Surftype = SOP_TorusEnums::Surftype;
    using Orient = SOP_TorusEnums::Orient;



    void        buildFromOp(const OP_Node *node, fpreal time, DEP_MicroNode *depnode)
    {
        if (true)
            OP_Utils::evalOpParm(myType, node, "type", time, 0);
        else myType = 0;
        if (true)
            OP_Utils::evalOpParm(mySurftype, node, "surftype", time, 0);
        else mySurftype = 4;
        if (true)
            OP_Utils::evalOpParm(myOrient, node, "orient", time, 0);
        else myOrient = 1;
        if (true)
            OP_Utils::evalOpParm(myRad, node, "rad", time, 0);
        else myRad = UT_Vector2D(1,0.5);
        if (true)
            OP_Utils::evalOpParm(myT, node, "t", time, 0);
        else myT = UT_Vector3D(0,0,0);
        if (true)
            OP_Utils::evalOpParm(myR, node, "r", time, 0);
        else myR = UT_Vector3D(0,0,0);
        if (true)
            OP_Utils::evalOpParm(myScale, node, "scale", time, 0);
        else myScale = 1;
        if (true && ( (!(((int64(getType())!=3)&&(int64(getType())!=2)))) ) )
            OP_Utils::evalOpParm(myImperfect, node, "imperfect", time, 0);
        else myImperfect = true;
        if (true && ( (!(((int64(getType())!=3)&&(int64(getType())!=2)))) ) )
            OP_Utils::evalOpParm(myOrderu, node, "orderu", time, 0);
        else myOrderu = 4;
        if (true && ( (!(((int64(getType())!=3)&&(int64(getType())!=2)))) ) )
            OP_Utils::evalOpParm(myOrderv, node, "orderv", time, 0);
        else myOrderv = 4;
        if (true && ( (!(((int64(getType())==2)&&(getImperfect()==0))||((int64(getType())==3)&&(getImperfect()==0)))) ) )
            OP_Utils::evalOpParm(myCloseu, node, "closeu", time, 0);
        else myCloseu = true;
        if (true && ( (!(((int64(getType())==2)&&(getImperfect()==0))||((int64(getType())==3)&&(getImperfect()==0)))) ) )
            OP_Utils::evalOpParm(myClosev, node, "closev", time, 0);
        else myClosev = true;
        if (true)
            OP_Utils::evalOpParm(myCapu, node, "capu", time, 0);
        else myCapu = false;
        if (true)
            OP_Utils::evalOpParm(myCapv, node, "capv", time, 0);
        else myCapv = false;
        if (true && ( (!(((int64(getType())==2)&&(getImperfect()==0))||((int64(getType())==3)&&(getImperfect()==0)))) ) )
            OP_Utils::evalOpParm(myRows, node, "rows", time, 0);
        else myRows = 12;
        if (true && ( (!(((int64(getType())==2)&&(getImperfect()==0))||((int64(getType())==3)&&(getImperfect()==0)))) ) )
            OP_Utils::evalOpParm(myCols, node, "cols", time, 0);
        else myCols = 24;
        if (true && ( (!(((getCloseu()==1)))) ) )
            OP_Utils::evalOpParm(myAngleu, node, "angleu", time, 0);
        else myAngleu = UT_Vector2D(0,360);
        if (true && ( (!(((getClosev()==1)))) ) )
            OP_Utils::evalOpParm(myAnglev, node, "anglev", time, 0);
        else myAnglev = UT_Vector2D(0,360);

    }


    virtual void loadFromOpSubclass(const LoadParms &loadparms)
    {
        buildFromOp(loadparms.node(), loadparms.context().getTime(), loadparms.depnode());
    }


    virtual void copyFrom(const SOP_NodeParms *src)
    {
        *this = *((const SOP_TorusParms *)src);
    }

    template <typename T>
    void
    doGetParmValue(exint idx, T &value) const
    {
        switch (idx)
        {
            case 0:
                coerceValue(value, myType);
                break;
            case 1:
                coerceValue(value, mySurftype);
                break;
            case 2:
                coerceValue(value, myOrient);
                break;
            case 3:
                coerceValue(value, myRad);
                break;
            case 4:
                coerceValue(value, myT);
                break;
            case 5:
                coerceValue(value, myR);
                break;
            case 6:
                coerceValue(value, myScale);
                break;
            case 7:
                coerceValue(value, myImperfect);
                break;
            case 8:
                coerceValue(value, myOrderu);
                break;
            case 9:
                coerceValue(value, myOrderv);
                break;
            case 10:
                coerceValue(value, myCloseu);
                break;
            case 11:
                coerceValue(value, myClosev);
                break;
            case 12:
                coerceValue(value, myCapu);
                break;
            case 13:
                coerceValue(value, myCapv);
                break;
            case 14:
                coerceValue(value, myRows);
                break;
            case 15:
                coerceValue(value, myCols);
                break;
            case 16:
                coerceValue(value, myAngleu);
                break;
            case 17:
                coerceValue(value, myAnglev);
                break;

        }
    }

    void        getParmValue(exint idx, exint &value) const
    { doGetParmValue(idx, value); }
    void        getParmValue(exint idx, fpreal &value) const
    { doGetParmValue(idx, value); }
    void        getParmValue(exint idx, UT_Vector2D &value) const
    { doGetParmValue(idx, value); }
    void        getParmValue(exint idx, UT_Vector3D &value) const
    { doGetParmValue(idx, value); }
    void        getParmValue(exint idx, UT_Vector4D &value) const
    { doGetParmValue(idx, value); }
    void        getParmValue(exint idx, UT_Matrix2D &value) const
    { doGetParmValue(idx, value); }
    void        getParmValue(exint idx, UT_Matrix3D &value) const
    { doGetParmValue(idx, value); }
    void        getParmValue(exint idx, UT_Matrix4D &value) const
    { doGetParmValue(idx, value); }
    void        getParmValue(exint idx, UT_StringHolder &value) const
    { doGetParmValue(idx, value); }
    void        getParmValue(exint idx, UT_SharedPtr<UT_Ramp> &value) const
    { doGetParmValue(idx, value); }
    void        getParmValue(exint idx, PRM_DataItemHandle &value) const
    { doGetParmValue(idx, value); }

    template <typename T>
    void
    doSetParmValue(exint idx, const T &value) 
    {
        switch (idx)
        {
            case 0:
                coerceValue(myType, value);
                break;
            case 1:
                coerceValue(mySurftype, value);
                break;
            case 2:
                coerceValue(myOrient, value);
                break;
            case 3:
                coerceValue(myRad, value);
                break;
            case 4:
                coerceValue(myT, value);
                break;
            case 5:
                coerceValue(myR, value);
                break;
            case 6:
                coerceValue(myScale, value);
                break;
            case 7:
                coerceValue(myImperfect, value);
                break;
            case 8:
                coerceValue(myOrderu, value);
                break;
            case 9:
                coerceValue(myOrderv, value);
                break;
            case 10:
                coerceValue(myCloseu, value);
                break;
            case 11:
                coerceValue(myClosev, value);
                break;
            case 12:
                coerceValue(myCapu, value);
                break;
            case 13:
                coerceValue(myCapv, value);
                break;
            case 14:
                coerceValue(myRows, value);
                break;
            case 15:
                coerceValue(myCols, value);
                break;
            case 16:
                coerceValue(myAngleu, value);
                break;
            case 17:
                coerceValue(myAnglev, value);
                break;

        }
    }

    void        setParmValue(exint idx, const exint &value) 
    { doSetParmValue(idx, value); }
    void        setParmValue(exint idx, const fpreal &value) 
    { doSetParmValue(idx, value); }
    void        setParmValue(exint idx, const UT_Vector2D &value) 
    { doSetParmValue(idx, value); }
    void        setParmValue(exint idx, const UT_Vector3D &value) 
    { doSetParmValue(idx, value); }
    void        setParmValue(exint idx, const UT_Vector4D &value) 
    { doSetParmValue(idx, value); }
    void        setParmValue(exint idx, const UT_Matrix2D &value) 
    { doSetParmValue(idx, value); }
    void        setParmValue(exint idx, const UT_Matrix3D &value) 
    { doSetParmValue(idx, value); }
    void        setParmValue(exint idx, const UT_Matrix4D &value) 
    { doSetParmValue(idx, value); }
    void        setParmValue(exint idx, const UT_StringHolder &value) 
    { doSetParmValue(idx, value); }
    void        setParmValue(exint idx, const UT_SharedPtr<UT_Ramp> &value) 
    { doSetParmValue(idx, value); }
    void        setParmValue(exint idx, const PRM_DataItemHandle &value) 
    { doSetParmValue(idx, value); }

    virtual exint    getNumParms() const
    {
        return 18;
    }

    virtual const char *getParmName(exint fieldnum) const
    {
        switch (fieldnum)
        {
            case 0:
                return "type";
            case 1:
                return "surftype";
            case 2:
                return "orient";
            case 3:
                return "rad";
            case 4:
                return "t";
            case 5:
                return "r";
            case 6:
                return "scale";
            case 7:
                return "imperfect";
            case 8:
                return "orderu";
            case 9:
                return "orderv";
            case 10:
                return "closeu";
            case 11:
                return "closev";
            case 12:
                return "capu";
            case 13:
                return "capv";
            case 14:
                return "rows";
            case 15:
                return "cols";
            case 16:
                return "angleu";
            case 17:
                return "anglev";

        }
        return 0;
    }

    virtual ParmType getParmType(exint fieldnum) const
    {
        switch (fieldnum)
        {
                case 0:
                    return PARM_INTEGER;
                case 1:
                    return PARM_INTEGER;
                case 2:
                    return PARM_INTEGER;
                case 3:
                    return PARM_VECTOR2;
                case 4:
                    return PARM_VECTOR3;
                case 5:
                    return PARM_VECTOR3;
                case 6:
                    return PARM_FLOAT;
                case 7:
                    return PARM_INTEGER;
                case 8:
                    return PARM_INTEGER;
                case 9:
                    return PARM_INTEGER;
                case 10:
                    return PARM_INTEGER;
                case 11:
                    return PARM_INTEGER;
                case 12:
                    return PARM_INTEGER;
                case 13:
                    return PARM_INTEGER;
                case 14:
                    return PARM_INTEGER;
                case 15:
                    return PARM_INTEGER;
                case 16:
                    return PARM_VECTOR2;
                case 17:
                    return PARM_VECTOR2;

        }
        return PARM_UNSUPPORTED;
    }

    // Boiler plate to load individual types.
    static void  loadData(UT_IStream &is, int64 &v)
    { is.bread(&v, 1); }
    static void  loadData(UT_IStream &is, bool &v)
    { int64     iv; is.bread(&iv, 1); v = iv; }
    static void  loadData(UT_IStream &is, fpreal64 &v)
    { is.bread<fpreal64>(&v, 1); }
    static void  loadData(UT_IStream &is, UT_Vector2D &v)
    { is.bread<fpreal64>(&v.x(), 1); is.bread<fpreal64>(&v.y(), 1); }
    static void  loadData(UT_IStream &is, UT_Vector3D &v)
    { is.bread<fpreal64>(&v.x(), 1); is.bread<fpreal64>(&v.y(), 1);
      is.bread<fpreal64>(&v.z(), 1); }
    static void  loadData(UT_IStream &is, UT_Vector4D &v)
    { is.bread<fpreal64>(&v.x(), 1); is.bread<fpreal64>(&v.y(), 1);
      is.bread<fpreal64>(&v.z(), 1); is.bread<fpreal64>(&v.w(), 1); }
    static void  loadData(UT_IStream &is, UT_Matrix2D &v)
    { for (int r = 0; r < 2; r++) for (int c = 0; c < 2; c++) is.bread<fpreal64>(&v(r, c), 1); }
    static void  loadData(UT_IStream &is, UT_Matrix3D &v)
    { for (int r = 0; r < 3; r++) for (int c = 0; c < 3; c++) is.bread<fpreal64>(&v(r, c), 1); }
    static void  loadData(UT_IStream &is, UT_Matrix4D &v)
    { for (int r = 0; r < 4; r++) for (int c = 0; c < 4; c++) is.bread<fpreal64>(&v(r, c), 1); }
    static void  loadData(UT_IStream &is, UT_Vector2I &v)
    { is.bread<int64>(&v.x(), 1); is.bread<int64>(&v.y(), 1); }
    static void  loadData(UT_IStream &is, UT_Vector3I &v)
    { is.bread<int64>(&v.x(), 1); is.bread<int64>(&v.y(), 1);
      is.bread<int64>(&v.z(), 1); }
    static void  loadData(UT_IStream &is, UT_Vector4I &v)
    { is.bread<int64>(&v.x(), 1); is.bread<int64>(&v.y(), 1);
      is.bread<int64>(&v.z(), 1); is.bread<int64>(&v.w(), 1); }
    static void  loadData(UT_IStream &is, UT_StringHolder &v)
    { is.bread(v); }
    static void  loadData(UT_IStream &is, UT_SharedPtr<UT_Ramp> &v)
    {   UT_StringHolder   rampdata;
        loadData(is, rampdata);
        if (rampdata.isstring())
        {
            v.reset(new UT_Ramp());
            UT_IStream  istr((const char *) rampdata, rampdata.length(), UT_ISTREAM_ASCII);
            v->load(istr);
        }
        else v.reset();
    }
    static void  loadData(UT_IStream &is, PRM_DataItemHandle &v)
    {   UT_StringHolder   data;
        loadData(is, data);
        if (data.isstring())
        {
            // Find the data type.
            char *colon = UT_StringWrap(data).findChar(':');
            if (colon)
            {
                int             typelen = colon - data.buffer();
                UT_WorkBuffer   type;
                type.strncpy(data.buffer(), typelen);
                UT_IStream  istr(((const char *) data) + typelen + 1, data.length() - (typelen + 1), UT_ISTREAM_BINARY);
                
                v = PRM_DataFactory::parseBinary(type.buffer(), istr);
            }
        }
        else v.reset();
    }

    static void  saveData(std::ostream &os, int64 v)
    { UTwrite(os, &v); }
    static void  saveData(std::ostream &os, bool v)
    { int64 iv = v; UTwrite(os, &iv); }
    static void  saveData(std::ostream &os, fpreal64 v)
    { UTwrite<fpreal64>(os, &v); }
    static void  saveData(std::ostream &os, UT_Vector2D v)
    { UTwrite<fpreal64>(os, &v.x()); UTwrite<fpreal64>(os, &v.y()); }
    static void  saveData(std::ostream &os, UT_Vector3D v)
    { UTwrite<fpreal64>(os, &v.x()); UTwrite<fpreal64>(os, &v.y());
      UTwrite<fpreal64>(os, &v.z()); }
    static void  saveData(std::ostream &os, UT_Vector4D v)
    { UTwrite<fpreal64>(os, &v.x()); UTwrite<fpreal64>(os, &v.y());
      UTwrite<fpreal64>(os, &v.z()); UTwrite<fpreal64>(os, &v.w()); }
    static void  saveData(std::ostream &os, UT_Matrix2D v)
    { for (int r = 0; r < 2; r++) for (int c = 0; c < 2; c++) UTwrite<fpreal64>(os, &v(r, c)); }
    static void  saveData(std::ostream &os, UT_Matrix3D v)
    { for (int r = 0; r < 3; r++) for (int c = 0; c < 3; c++) UTwrite<fpreal64>(os, &v(r, c)); }
    static void  saveData(std::ostream &os, UT_Matrix4D v)
    { for (int r = 0; r < 4; r++) for (int c = 0; c < 4; c++) UTwrite<fpreal64>(os, &v(r, c)); }
    static void  saveData(std::ostream &os, UT_StringHolder s)
    { UT_StringWrap(s).saveBinary(os); }
    static void  saveData(std::ostream &os, UT_SharedPtr<UT_Ramp> s)
    {   UT_StringHolder         result;
        UT_OStringStream        ostr;
        if (s) s->save(ostr);
        result = ostr.str();
        saveData(os, result);
    }
    static void  saveData(std::ostream &os, PRM_DataItemHandle s)
    {   UT_StringHolder         result;
        UT_OStringStream        ostr;
        if (s) 
        {
            ostr << s->getDataTypeToken();
            ostr << ":";
            s->saveBinary(ostr);
        }
        result = ostr.str();
        saveData(os, result);
    }


    void         save(std::ostream &os) const
    {
        int32           v = version();
        UTwrite(os, &v);
        saveData(os, myType);
        saveData(os, mySurftype);
        saveData(os, myOrient);
        saveData(os, myRad);
        saveData(os, myT);
        saveData(os, myR);
        saveData(os, myScale);
        saveData(os, myImperfect);
        saveData(os, myOrderu);
        saveData(os, myOrderv);
        saveData(os, myCloseu);
        saveData(os, myClosev);
        saveData(os, myCapu);
        saveData(os, myCapv);
        saveData(os, myRows);
        saveData(os, myCols);
        saveData(os, myAngleu);
        saveData(os, myAnglev);

    }

    bool         load(UT_IStream &is)
    {
        int32           v;
        is.bread(&v, 1);
        if (version() != v)
        {
            // Fail incompatible versions
            return false;
        }
        loadData(is, myType);
        loadData(is, mySurftype);
        loadData(is, myOrient);
        loadData(is, myRad);
        loadData(is, myT);
        loadData(is, myR);
        loadData(is, myScale);
        loadData(is, myImperfect);
        loadData(is, myOrderu);
        loadData(is, myOrderv);
        loadData(is, myCloseu);
        loadData(is, myClosev);
        loadData(is, myCapu);
        loadData(is, myCapv);
        loadData(is, myRows);
        loadData(is, myCols);
        loadData(is, myAngleu);
        loadData(is, myAnglev);

        return true;
    }

    Type getType() const { return Type(myType); }
    void setType(Type val) { myType = int64(val); }
    Type opType(const SOP_NodeVerb::CookParms &cookparms) const
    { 
        SOP_Node *thissop = cookparms.getNode();
        if (!thissop) return getType();
        int64 result;
        OP_Utils::evalOpParm(result, thissop, "type", cookparms.getCookTime(), 0);
        return Type(result);
    }
    Surftype getSurftype() const { return Surftype(mySurftype); }
    void setSurftype(Surftype val) { mySurftype = int64(val); }
    Surftype opSurftype(const SOP_NodeVerb::CookParms &cookparms) const
    { 
        SOP_Node *thissop = cookparms.getNode();
        if (!thissop) return getSurftype();
        int64 result;
        OP_Utils::evalOpParm(result, thissop, "surftype", cookparms.getCookTime(), 0);
        return Surftype(result);
    }
    Orient getOrient() const { return Orient(myOrient); }
    void setOrient(Orient val) { myOrient = int64(val); }
    Orient opOrient(const SOP_NodeVerb::CookParms &cookparms) const
    { 
        SOP_Node *thissop = cookparms.getNode();
        if (!thissop) return getOrient();
        int64 result;
        OP_Utils::evalOpParm(result, thissop, "orient", cookparms.getCookTime(), 0);
        return Orient(result);
    }
    UT_Vector2D getRad() const { return myRad; }
    void setRad(UT_Vector2D val) { myRad = val; }
    UT_Vector2D opRad(const SOP_NodeVerb::CookParms &cookparms) const
    { 
        SOP_Node *thissop = cookparms.getNode();
        if (!thissop) return getRad();
        UT_Vector2D result;
        OP_Utils::evalOpParm(result, thissop, "rad", cookparms.getCookTime(), 0);
        return result;
    }
    UT_Vector3D getT() const { return myT; }
    void setT(UT_Vector3D val) { myT = val; }
    UT_Vector3D opT(const SOP_NodeVerb::CookParms &cookparms) const
    { 
        SOP_Node *thissop = cookparms.getNode();
        if (!thissop) return getT();
        UT_Vector3D result;
        OP_Utils::evalOpParm(result, thissop, "t", cookparms.getCookTime(), 0);
        return result;
    }
    UT_Vector3D getR() const { return myR; }
    void setR(UT_Vector3D val) { myR = val; }
    UT_Vector3D opR(const SOP_NodeVerb::CookParms &cookparms) const
    { 
        SOP_Node *thissop = cookparms.getNode();
        if (!thissop) return getR();
        UT_Vector3D result;
        OP_Utils::evalOpParm(result, thissop, "r", cookparms.getCookTime(), 0);
        return result;
    }
    fpreal64 getScale() const { return myScale; }
    void setScale(fpreal64 val) { myScale = val; }
    fpreal64 opScale(const SOP_NodeVerb::CookParms &cookparms) const
    { 
        SOP_Node *thissop = cookparms.getNode();
        if (!thissop) return getScale();
        fpreal64 result;
        OP_Utils::evalOpParm(result, thissop, "scale", cookparms.getCookTime(), 0);
        return result;
    }
    bool getImperfect() const { return myImperfect; }
    void setImperfect(bool val) { myImperfect = val; }
    bool opImperfect(const SOP_NodeVerb::CookParms &cookparms) const
    { 
        SOP_Node *thissop = cookparms.getNode();
        if (!thissop) return getImperfect();
        bool result;
        OP_Utils::evalOpParm(result, thissop, "imperfect", cookparms.getCookTime(), 0);
        return result;
    }
    int64 getOrderu() const { return myOrderu; }
    void setOrderu(int64 val) { myOrderu = val; }
    int64 opOrderu(const SOP_NodeVerb::CookParms &cookparms) const
    { 
        SOP_Node *thissop = cookparms.getNode();
        if (!thissop) return getOrderu();
        int64 result;
        OP_Utils::evalOpParm(result, thissop, "orderu", cookparms.getCookTime(), 0);
        return result;
    }
    int64 getOrderv() const { return myOrderv; }
    void setOrderv(int64 val) { myOrderv = val; }
    int64 opOrderv(const SOP_NodeVerb::CookParms &cookparms) const
    { 
        SOP_Node *thissop = cookparms.getNode();
        if (!thissop) return getOrderv();
        int64 result;
        OP_Utils::evalOpParm(result, thissop, "orderv", cookparms.getCookTime(), 0);
        return result;
    }
    bool getCloseu() const { return myCloseu; }
    void setCloseu(bool val) { myCloseu = val; }
    bool opCloseu(const SOP_NodeVerb::CookParms &cookparms) const
    { 
        SOP_Node *thissop = cookparms.getNode();
        if (!thissop) return getCloseu();
        bool result;
        OP_Utils::evalOpParm(result, thissop, "closeu", cookparms.getCookTime(), 0);
        return result;
    }
    bool getClosev() const { return myClosev; }
    void setClosev(bool val) { myClosev = val; }
    bool opClosev(const SOP_NodeVerb::CookParms &cookparms) const
    { 
        SOP_Node *thissop = cookparms.getNode();
        if (!thissop) return getClosev();
        bool result;
        OP_Utils::evalOpParm(result, thissop, "closev", cookparms.getCookTime(), 0);
        return result;
    }
    bool getCapu() const { return myCapu; }
    void setCapu(bool val) { myCapu = val; }
    bool opCapu(const SOP_NodeVerb::CookParms &cookparms) const
    { 
        SOP_Node *thissop = cookparms.getNode();
        if (!thissop) return getCapu();
        bool result;
        OP_Utils::evalOpParm(result, thissop, "capu", cookparms.getCookTime(), 0);
        return result;
    }
    bool getCapv() const { return myCapv; }
    void setCapv(bool val) { myCapv = val; }
    bool opCapv(const SOP_NodeVerb::CookParms &cookparms) const
    { 
        SOP_Node *thissop = cookparms.getNode();
        if (!thissop) return getCapv();
        bool result;
        OP_Utils::evalOpParm(result, thissop, "capv", cookparms.getCookTime(), 0);
        return result;
    }
    int64 getRows() const { return myRows; }
    void setRows(int64 val) { myRows = val; }
    int64 opRows(const SOP_NodeVerb::CookParms &cookparms) const
    { 
        SOP_Node *thissop = cookparms.getNode();
        if (!thissop) return getRows();
        int64 result;
        OP_Utils::evalOpParm(result, thissop, "rows", cookparms.getCookTime(), 0);
        return result;
    }
    int64 getCols() const { return myCols; }
    void setCols(int64 val) { myCols = val; }
    int64 opCols(const SOP_NodeVerb::CookParms &cookparms) const
    { 
        SOP_Node *thissop = cookparms.getNode();
        if (!thissop) return getCols();
        int64 result;
        OP_Utils::evalOpParm(result, thissop, "cols", cookparms.getCookTime(), 0);
        return result;
    }
    UT_Vector2D getAngleu() const { return myAngleu; }
    void setAngleu(UT_Vector2D val) { myAngleu = val; }
    UT_Vector2D opAngleu(const SOP_NodeVerb::CookParms &cookparms) const
    { 
        SOP_Node *thissop = cookparms.getNode();
        if (!thissop) return getAngleu();
        UT_Vector2D result;
        OP_Utils::evalOpParm(result, thissop, "angleu", cookparms.getCookTime(), 0);
        return result;
    }
    UT_Vector2D getAnglev() const { return myAnglev; }
    void setAnglev(UT_Vector2D val) { myAnglev = val; }
    UT_Vector2D opAnglev(const SOP_NodeVerb::CookParms &cookparms) const
    { 
        SOP_Node *thissop = cookparms.getNode();
        if (!thissop) return getAnglev();
        UT_Vector2D result;
        OP_Utils::evalOpParm(result, thissop, "anglev", cookparms.getCookTime(), 0);
        return result;
    }

private:
    int64 myType;
    int64 mySurftype;
    int64 myOrient;
    UT_Vector2D myRad;
    UT_Vector3D myT;
    UT_Vector3D myR;
    fpreal64 myScale;
    bool myImperfect;
    int64 myOrderu;
    int64 myOrderv;
    bool myCloseu;
    bool myClosev;
    bool myCapu;
    bool myCapv;
    int64 myRows;
    int64 myCols;
    UT_Vector2D myAngleu;
    UT_Vector2D myAnglev;

};
