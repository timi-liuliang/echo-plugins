/* Automagically Generated by generate_proto.py
 * Do not Edit
 */
#pragma once

#include <SOP/SOP_NodeVerb.h>
#include <OP/OP_Utils.h>
#include <PRM/PRM_Parm.h>
#include <UT/UT_IStream.h>
#include <UT/UT_NTStreamUtil.h>
#include <UT/UT_Ramp.h>
#include <UT/UT_SharedPtr.h>
#include <UT/UT_StringHolder.h>
#include <UT/UT_StringStream.h>
#include <UT/UT_VectorTypes.h>
#include <SYS/SYS_Types.h>

using namespace UT::Literal;

class DEP_MicroNode;
namespace SOP_CaptureAttribUnpackEnums
{
    enum class Class
    {
        DETAIL = 0,
        PRIMITIVE,
        POINT,
        VERTEX
    };
}


class SOP_API SOP_CaptureAttribUnpackParms  : public SOP_NodeParms
{
public:
    static int version() { return 1; }

    SOP_CaptureAttribUnpackParms()
    {
        myClass = 2;
        myAttrib = "boneCapture"_sh;
        myPrefix = ""_sh;
        mySecondaryPrefix = "secondary"_sh;
        myUnpackProperties = false;
        myUnpackData = false;
        myDeleteCaptureAttributes = false;

    }

    explicit SOP_CaptureAttribUnpackParms(const SOP_CaptureAttribUnpackParms &) = default;

    virtual ~SOP_CaptureAttribUnpackParms() {}

    bool operator==(const SOP_CaptureAttribUnpackParms &src) const
    {
        if (myClass != src.myClass) return false;
        if (myAttrib != src.myAttrib) return false;
        if (myPrefix != src.myPrefix) return false;
        if (mySecondaryPrefix != src.mySecondaryPrefix) return false;
        if (myUnpackProperties != src.myUnpackProperties) return false;
        if (myUnpackData != src.myUnpackData) return false;
        if (myDeleteCaptureAttributes != src.myDeleteCaptureAttributes) return false;

        return true;
    }
    bool operator!=(const SOP_CaptureAttribUnpackParms &src) const
    {
        return !operator==(src);
    }
    using Class = SOP_CaptureAttribUnpackEnums::Class;



    void        buildFromOp(const OP_Node *node, fpreal time, DEP_MicroNode *depnode)
    {
        if (true)
            OP_Utils::evalOpParm(myClass, node, "class", time, 0);
        else myClass = 2;
        if (true)
            OP_Utils::evalOpParm(myAttrib, node, "attrib", time, 0);
        else myAttrib = "boneCapture"_sh;
        if (true)
            OP_Utils::evalOpParm(myPrefix, node, "prefix", time, 0);
        else myPrefix = ""_sh;
        if (true)
            OP_Utils::evalOpParm(mySecondaryPrefix, node, "secondaryprefix", time, 0);
        else mySecondaryPrefix = "secondary"_sh;
        if (true)
            OP_Utils::evalOpParm(myUnpackProperties, node, "unpackproperties", time, 0);
        else myUnpackProperties = false;
        if (true)
            OP_Utils::evalOpParm(myUnpackData, node, "unpackdata", time, 0);
        else myUnpackData = false;
        if (true)
            OP_Utils::evalOpParm(myDeleteCaptureAttributes, node, "deletecapture", time, 0);
        else myDeleteCaptureAttributes = false;

    }


    virtual void loadFromOpSubclass(const LoadParms &loadparms)
    {
        buildFromOp(loadparms.node(), loadparms.context().getTime(), loadparms.depnode());
    }


    virtual void copyFrom(const SOP_NodeParms *src)
    {
        *this = *((const SOP_CaptureAttribUnpackParms *)src);
    }

    template <typename T>
    void
    doGetParmValue(exint idx, T &value) const
    {
        switch (idx)
        {
            case 0:
                coerceValue(value, myClass);
                break;
            case 1:
                coerceValue(value, myAttrib);
                break;
            case 2:
                coerceValue(value, myPrefix);
                break;
            case 3:
                coerceValue(value, mySecondaryPrefix);
                break;
            case 4:
                coerceValue(value, myUnpackProperties);
                break;
            case 5:
                coerceValue(value, myUnpackData);
                break;
            case 6:
                coerceValue(value, myDeleteCaptureAttributes);
                break;

        }
    }

    void        getParmValue(exint idx, exint &value) const
    { doGetParmValue(idx, value); }
    void        getParmValue(exint idx, fpreal &value) const
    { doGetParmValue(idx, value); }
    void        getParmValue(exint idx, UT_Vector2D &value) const
    { doGetParmValue(idx, value); }
    void        getParmValue(exint idx, UT_Vector3D &value) const
    { doGetParmValue(idx, value); }
    void        getParmValue(exint idx, UT_Vector4D &value) const
    { doGetParmValue(idx, value); }
    void        getParmValue(exint idx, UT_Matrix2D &value) const
    { doGetParmValue(idx, value); }
    void        getParmValue(exint idx, UT_Matrix3D &value) const
    { doGetParmValue(idx, value); }
    void        getParmValue(exint idx, UT_Matrix4D &value) const
    { doGetParmValue(idx, value); }
    void        getParmValue(exint idx, UT_StringHolder &value) const
    { doGetParmValue(idx, value); }
    void        getParmValue(exint idx, UT_SharedPtr<UT_Ramp> &value) const
    { doGetParmValue(idx, value); }
    void        getParmValue(exint idx, PRM_DataItemHandle &value) const
    { doGetParmValue(idx, value); }

    template <typename T>
    void
    doSetParmValue(exint idx, const T &value) 
    {
        switch (idx)
        {
            case 0:
                coerceValue(myClass, value);
                break;
            case 1:
                coerceValue(myAttrib, value);
                break;
            case 2:
                coerceValue(myPrefix, value);
                break;
            case 3:
                coerceValue(mySecondaryPrefix, value);
                break;
            case 4:
                coerceValue(myUnpackProperties, value);
                break;
            case 5:
                coerceValue(myUnpackData, value);
                break;
            case 6:
                coerceValue(myDeleteCaptureAttributes, value);
                break;

        }
    }

    void        setParmValue(exint idx, const exint &value) 
    { doSetParmValue(idx, value); }
    void        setParmValue(exint idx, const fpreal &value) 
    { doSetParmValue(idx, value); }
    void        setParmValue(exint idx, const UT_Vector2D &value) 
    { doSetParmValue(idx, value); }
    void        setParmValue(exint idx, const UT_Vector3D &value) 
    { doSetParmValue(idx, value); }
    void        setParmValue(exint idx, const UT_Vector4D &value) 
    { doSetParmValue(idx, value); }
    void        setParmValue(exint idx, const UT_Matrix2D &value) 
    { doSetParmValue(idx, value); }
    void        setParmValue(exint idx, const UT_Matrix3D &value) 
    { doSetParmValue(idx, value); }
    void        setParmValue(exint idx, const UT_Matrix4D &value) 
    { doSetParmValue(idx, value); }
    void        setParmValue(exint idx, const UT_StringHolder &value) 
    { doSetParmValue(idx, value); }
    void        setParmValue(exint idx, const UT_SharedPtr<UT_Ramp> &value) 
    { doSetParmValue(idx, value); }
    void        setParmValue(exint idx, const PRM_DataItemHandle &value) 
    { doSetParmValue(idx, value); }

    virtual exint    getNumParms() const
    {
        return 7;
    }

    virtual const char *getParmName(exint fieldnum) const
    {
        switch (fieldnum)
        {
            case 0:
                return "class";
            case 1:
                return "attrib";
            case 2:
                return "prefix";
            case 3:
                return "secondaryprefix";
            case 4:
                return "unpackproperties";
            case 5:
                return "unpackdata";
            case 6:
                return "deletecapture";

        }
        return 0;
    }

    virtual ParmType getParmType(exint fieldnum) const
    {
        switch (fieldnum)
        {
                case 0:
                    return PARM_INTEGER;
                case 1:
                    return PARM_STRING;
                case 2:
                    return PARM_STRING;
                case 3:
                    return PARM_STRING;
                case 4:
                    return PARM_INTEGER;
                case 5:
                    return PARM_INTEGER;
                case 6:
                    return PARM_INTEGER;

        }
        return PARM_UNSUPPORTED;
    }

    // Boiler plate to load individual types.
    static void  loadData(UT_IStream &is, int64 &v)
    { is.bread(&v, 1); }
    static void  loadData(UT_IStream &is, bool &v)
    { int64     iv; is.bread(&iv, 1); v = iv; }
    static void  loadData(UT_IStream &is, fpreal64 &v)
    { is.bread<fpreal64>(&v, 1); }
    static void  loadData(UT_IStream &is, UT_Vector2D &v)
    { is.bread<fpreal64>(&v.x(), 1); is.bread<fpreal64>(&v.y(), 1); }
    static void  loadData(UT_IStream &is, UT_Vector3D &v)
    { is.bread<fpreal64>(&v.x(), 1); is.bread<fpreal64>(&v.y(), 1);
      is.bread<fpreal64>(&v.z(), 1); }
    static void  loadData(UT_IStream &is, UT_Vector4D &v)
    { is.bread<fpreal64>(&v.x(), 1); is.bread<fpreal64>(&v.y(), 1);
      is.bread<fpreal64>(&v.z(), 1); is.bread<fpreal64>(&v.w(), 1); }
    static void  loadData(UT_IStream &is, UT_Matrix2D &v)
    { for (int r = 0; r < 2; r++) for (int c = 0; c < 2; c++) is.bread<fpreal64>(&v(r, c), 1); }
    static void  loadData(UT_IStream &is, UT_Matrix3D &v)
    { for (int r = 0; r < 3; r++) for (int c = 0; c < 3; c++) is.bread<fpreal64>(&v(r, c), 1); }
    static void  loadData(UT_IStream &is, UT_Matrix4D &v)
    { for (int r = 0; r < 4; r++) for (int c = 0; c < 4; c++) is.bread<fpreal64>(&v(r, c), 1); }
    static void  loadData(UT_IStream &is, UT_Vector2I &v)
    { is.bread<int64>(&v.x(), 1); is.bread<int64>(&v.y(), 1); }
    static void  loadData(UT_IStream &is, UT_Vector3I &v)
    { is.bread<int64>(&v.x(), 1); is.bread<int64>(&v.y(), 1);
      is.bread<int64>(&v.z(), 1); }
    static void  loadData(UT_IStream &is, UT_Vector4I &v)
    { is.bread<int64>(&v.x(), 1); is.bread<int64>(&v.y(), 1);
      is.bread<int64>(&v.z(), 1); is.bread<int64>(&v.w(), 1); }
    static void  loadData(UT_IStream &is, UT_StringHolder &v)
    { is.bread(v); }
    static void  loadData(UT_IStream &is, UT_SharedPtr<UT_Ramp> &v)
    {   UT_StringHolder   rampdata;
        loadData(is, rampdata);
        if (rampdata.isstring())
        {
            v.reset(new UT_Ramp());
            UT_IStream  istr((const char *) rampdata, rampdata.length(), UT_ISTREAM_ASCII);
            v->load(istr);
        }
        else v.reset();
    }
    static void  loadData(UT_IStream &is, PRM_DataItemHandle &v)
    {   UT_StringHolder   data;
        loadData(is, data);
        if (data.isstring())
        {
            // Find the data type.
            char *colon = UT_StringWrap(data).findChar(':');
            if (colon)
            {
                int             typelen = colon - data.buffer();
                UT_WorkBuffer   type;
                type.strncpy(data.buffer(), typelen);
                UT_IStream  istr(((const char *) data) + typelen + 1, data.length() - (typelen + 1), UT_ISTREAM_BINARY);
                
                v = PRM_DataFactory::parseBinary(type.buffer(), istr);
            }
        }
        else v.reset();
    }

    static void  saveData(std::ostream &os, int64 v)
    { UTwrite(os, &v); }
    static void  saveData(std::ostream &os, bool v)
    { int64 iv = v; UTwrite(os, &iv); }
    static void  saveData(std::ostream &os, fpreal64 v)
    { UTwrite<fpreal64>(os, &v); }
    static void  saveData(std::ostream &os, UT_Vector2D v)
    { UTwrite<fpreal64>(os, &v.x()); UTwrite<fpreal64>(os, &v.y()); }
    static void  saveData(std::ostream &os, UT_Vector3D v)
    { UTwrite<fpreal64>(os, &v.x()); UTwrite<fpreal64>(os, &v.y());
      UTwrite<fpreal64>(os, &v.z()); }
    static void  saveData(std::ostream &os, UT_Vector4D v)
    { UTwrite<fpreal64>(os, &v.x()); UTwrite<fpreal64>(os, &v.y());
      UTwrite<fpreal64>(os, &v.z()); UTwrite<fpreal64>(os, &v.w()); }
    static void  saveData(std::ostream &os, UT_Matrix2D v)
    { for (int r = 0; r < 2; r++) for (int c = 0; c < 2; c++) UTwrite<fpreal64>(os, &v(r, c)); }
    static void  saveData(std::ostream &os, UT_Matrix3D v)
    { for (int r = 0; r < 3; r++) for (int c = 0; c < 3; c++) UTwrite<fpreal64>(os, &v(r, c)); }
    static void  saveData(std::ostream &os, UT_Matrix4D v)
    { for (int r = 0; r < 4; r++) for (int c = 0; c < 4; c++) UTwrite<fpreal64>(os, &v(r, c)); }
    static void  saveData(std::ostream &os, UT_StringHolder s)
    { UT_StringWrap(s).saveBinary(os); }
    static void  saveData(std::ostream &os, UT_SharedPtr<UT_Ramp> s)
    {   UT_StringHolder         result;
        UT_OStringStream        ostr;
        if (s) s->save(ostr);
        result = ostr.str();
        saveData(os, result);
    }
    static void  saveData(std::ostream &os, PRM_DataItemHandle s)
    {   UT_StringHolder         result;
        UT_OStringStream        ostr;
        if (s) 
        {
            ostr << s->getDataTypeToken();
            ostr << ":";
            s->saveBinary(ostr);
        }
        result = ostr.str();
        saveData(os, result);
    }


    void         save(std::ostream &os) const
    {
        int32           v = version();
        UTwrite(os, &v);
        saveData(os, myClass);
        saveData(os, myAttrib);
        saveData(os, myPrefix);
        saveData(os, mySecondaryPrefix);
        saveData(os, myUnpackProperties);
        saveData(os, myUnpackData);
        saveData(os, myDeleteCaptureAttributes);

    }

    bool         load(UT_IStream &is)
    {
        int32           v;
        is.bread(&v, 1);
        if (version() != v)
        {
            // Fail incompatible versions
            return false;
        }
        loadData(is, myClass);
        loadData(is, myAttrib);
        loadData(is, myPrefix);
        loadData(is, mySecondaryPrefix);
        loadData(is, myUnpackProperties);
        loadData(is, myUnpackData);
        loadData(is, myDeleteCaptureAttributes);

        return true;
    }

    Class getClass() const { return Class(myClass); }
    void setClass(Class val) { myClass = int64(val); }
    Class opClass(const SOP_NodeVerb::CookParms &cookparms) const
    { 
        SOP_Node *thissop = cookparms.getNode();
        if (!thissop) return getClass();
        int64 result;
        OP_Utils::evalOpParm(result, thissop, "class", cookparms.getCookTime(), 0);
        return Class(result);
    }
    const UT_StringHolder & getAttrib() const { return myAttrib; }
    void setAttrib(const UT_StringHolder & val) { myAttrib = val; }
    UT_StringHolder opAttrib(const SOP_NodeVerb::CookParms &cookparms) const
    { 
        SOP_Node *thissop = cookparms.getNode();
        if (!thissop) return getAttrib();
        UT_StringHolder result;
        OP_Utils::evalOpParm(result, thissop, "attrib", cookparms.getCookTime(), 0);
        return result;
    }
    const UT_StringHolder & getPrefix() const { return myPrefix; }
    void setPrefix(const UT_StringHolder & val) { myPrefix = val; }
    UT_StringHolder opPrefix(const SOP_NodeVerb::CookParms &cookparms) const
    { 
        SOP_Node *thissop = cookparms.getNode();
        if (!thissop) return getPrefix();
        UT_StringHolder result;
        OP_Utils::evalOpParm(result, thissop, "prefix", cookparms.getCookTime(), 0);
        return result;
    }
    const UT_StringHolder & getSecondaryPrefix() const { return mySecondaryPrefix; }
    void setSecondaryPrefix(const UT_StringHolder & val) { mySecondaryPrefix = val; }
    UT_StringHolder opSecondaryPrefix(const SOP_NodeVerb::CookParms &cookparms) const
    { 
        SOP_Node *thissop = cookparms.getNode();
        if (!thissop) return getSecondaryPrefix();
        UT_StringHolder result;
        OP_Utils::evalOpParm(result, thissop, "secondaryprefix", cookparms.getCookTime(), 0);
        return result;
    }
    bool getUnpackProperties() const { return myUnpackProperties; }
    void setUnpackProperties(bool val) { myUnpackProperties = val; }
    bool opUnpackProperties(const SOP_NodeVerb::CookParms &cookparms) const
    { 
        SOP_Node *thissop = cookparms.getNode();
        if (!thissop) return getUnpackProperties();
        bool result;
        OP_Utils::evalOpParm(result, thissop, "unpackproperties", cookparms.getCookTime(), 0);
        return result;
    }
    bool getUnpackData() const { return myUnpackData; }
    void setUnpackData(bool val) { myUnpackData = val; }
    bool opUnpackData(const SOP_NodeVerb::CookParms &cookparms) const
    { 
        SOP_Node *thissop = cookparms.getNode();
        if (!thissop) return getUnpackData();
        bool result;
        OP_Utils::evalOpParm(result, thissop, "unpackdata", cookparms.getCookTime(), 0);
        return result;
    }
    bool getDeleteCaptureAttributes() const { return myDeleteCaptureAttributes; }
    void setDeleteCaptureAttributes(bool val) { myDeleteCaptureAttributes = val; }
    bool opDeleteCaptureAttributes(const SOP_NodeVerb::CookParms &cookparms) const
    { 
        SOP_Node *thissop = cookparms.getNode();
        if (!thissop) return getDeleteCaptureAttributes();
        bool result;
        OP_Utils::evalOpParm(result, thissop, "deletecapture", cookparms.getCookTime(), 0);
        return result;
    }

private:
    int64 myClass;
    UT_StringHolder myAttrib;
    UT_StringHolder myPrefix;
    UT_StringHolder mySecondaryPrefix;
    bool myUnpackProperties;
    bool myUnpackData;
    bool myDeleteCaptureAttributes;

};
