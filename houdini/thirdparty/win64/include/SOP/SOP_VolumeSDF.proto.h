/* Automagically Generated by generate_proto.py
 * Do not Edit
 */
#pragma once

#include <SOP/SOP_NodeVerb.h>
#include <OP/OP_Utils.h>
#include <PRM/PRM_Parm.h>
#include <UT/UT_IStream.h>
#include <UT/UT_NTStreamUtil.h>
#include <UT/UT_Ramp.h>
#include <UT/UT_SharedPtr.h>
#include <UT/UT_StringHolder.h>
#include <UT/UT_StringStream.h>
#include <UT/UT_VectorTypes.h>
#include <SYS/SYS_Types.h>

using namespace UT::Literal;

class DEP_MicroNode;

class SOP_API SOP_VolumeSDFParms  : public SOP_NodeParms
{
public:
    static int version() { return 1; }

    SOP_VolumeSDFParms()
    {
        myGroup = ""_sh;
        myIso = 0;
        myInvert = false;
        myUsemaxdist = false;
        myMaxdist = 0.1;
        myRebuildwithfim = false;
        myFimtolerance = .01;
        myFimiterations = 100;

    }

    explicit SOP_VolumeSDFParms(const SOP_VolumeSDFParms &) = default;

    virtual ~SOP_VolumeSDFParms() {}

    bool operator==(const SOP_VolumeSDFParms &src) const
    {
        if (myGroup != src.myGroup) return false;
        if (myIso != src.myIso) return false;
        if (myInvert != src.myInvert) return false;
        if (myUsemaxdist != src.myUsemaxdist) return false;
        if (myMaxdist != src.myMaxdist) return false;
        if (myRebuildwithfim != src.myRebuildwithfim) return false;
        if (myFimtolerance != src.myFimtolerance) return false;
        if (myFimiterations != src.myFimiterations) return false;

        return true;
    }
    bool operator!=(const SOP_VolumeSDFParms &src) const
    {
        return !operator==(src);
    }



    void        buildFromOp(const OP_Node *node, fpreal time, DEP_MicroNode *depnode)
    {
        if (true)
            OP_Utils::evalOpParm(myGroup, node, "group", time, 0);
        else myGroup = ""_sh;
        if (true)
            OP_Utils::evalOpParm(myIso, node, "iso", time, 0);
        else myIso = 0;
        if (true)
            OP_Utils::evalOpParm(myInvert, node, "invert", time, 0);
        else myInvert = false;
        if (true)
            OP_Utils::evalOpParm(myUsemaxdist, node, "usemaxdist", time, 0);
        else myUsemaxdist = false;
        if (true && ( (!(((getUsemaxdist()==0)))) ) )
            OP_Utils::evalOpParm(myMaxdist, node, "maxdist", time, 0);
        else myMaxdist = 0.1;
        if (true)
            OP_Utils::evalOpParm(myRebuildwithfim, node, "rebuildwithfim", time, 0);
        else myRebuildwithfim = false;
        if (true && ( (!(((getRebuildwithfim()==0)))) ) )
            OP_Utils::evalOpParm(myFimtolerance, node, "fimtolerance", time, 0);
        else myFimtolerance = .01;
        if (true && ( (!(((getRebuildwithfim()==0)))) ) )
            OP_Utils::evalOpParm(myFimiterations, node, "fimiterations", time, 0);
        else myFimiterations = 100;

    }


    virtual void loadFromOpSubclass(const LoadParms &loadparms)
    {
        buildFromOp(loadparms.node(), loadparms.context().getTime(), loadparms.depnode());
    }


    virtual void copyFrom(const SOP_NodeParms *src)
    {
        *this = *((const SOP_VolumeSDFParms *)src);
    }

    template <typename T>
    void
    doGetParmValue(exint idx, T &value) const
    {
        switch (idx)
        {
            case 0:
                coerceValue(value, myGroup);
                break;
            case 1:
                coerceValue(value, myIso);
                break;
            case 2:
                coerceValue(value, myInvert);
                break;
            case 3:
                coerceValue(value, myUsemaxdist);
                break;
            case 4:
                coerceValue(value, myMaxdist);
                break;
            case 5:
                coerceValue(value, myRebuildwithfim);
                break;
            case 6:
                coerceValue(value, myFimtolerance);
                break;
            case 7:
                coerceValue(value, myFimiterations);
                break;

        }
    }

    void        getParmValue(exint idx, exint &value) const
    { doGetParmValue(idx, value); }
    void        getParmValue(exint idx, fpreal &value) const
    { doGetParmValue(idx, value); }
    void        getParmValue(exint idx, UT_Vector2D &value) const
    { doGetParmValue(idx, value); }
    void        getParmValue(exint idx, UT_Vector3D &value) const
    { doGetParmValue(idx, value); }
    void        getParmValue(exint idx, UT_Vector4D &value) const
    { doGetParmValue(idx, value); }
    void        getParmValue(exint idx, UT_Matrix2D &value) const
    { doGetParmValue(idx, value); }
    void        getParmValue(exint idx, UT_Matrix3D &value) const
    { doGetParmValue(idx, value); }
    void        getParmValue(exint idx, UT_Matrix4D &value) const
    { doGetParmValue(idx, value); }
    void        getParmValue(exint idx, UT_StringHolder &value) const
    { doGetParmValue(idx, value); }
    void        getParmValue(exint idx, UT_SharedPtr<UT_Ramp> &value) const
    { doGetParmValue(idx, value); }
    void        getParmValue(exint idx, PRM_DataItemHandle &value) const
    { doGetParmValue(idx, value); }

    template <typename T>
    void
    doSetParmValue(exint idx, const T &value) 
    {
        switch (idx)
        {
            case 0:
                coerceValue(myGroup, value);
                break;
            case 1:
                coerceValue(myIso, value);
                break;
            case 2:
                coerceValue(myInvert, value);
                break;
            case 3:
                coerceValue(myUsemaxdist, value);
                break;
            case 4:
                coerceValue(myMaxdist, value);
                break;
            case 5:
                coerceValue(myRebuildwithfim, value);
                break;
            case 6:
                coerceValue(myFimtolerance, value);
                break;
            case 7:
                coerceValue(myFimiterations, value);
                break;

        }
    }

    void        setParmValue(exint idx, const exint &value) 
    { doSetParmValue(idx, value); }
    void        setParmValue(exint idx, const fpreal &value) 
    { doSetParmValue(idx, value); }
    void        setParmValue(exint idx, const UT_Vector2D &value) 
    { doSetParmValue(idx, value); }
    void        setParmValue(exint idx, const UT_Vector3D &value) 
    { doSetParmValue(idx, value); }
    void        setParmValue(exint idx, const UT_Vector4D &value) 
    { doSetParmValue(idx, value); }
    void        setParmValue(exint idx, const UT_Matrix2D &value) 
    { doSetParmValue(idx, value); }
    void        setParmValue(exint idx, const UT_Matrix3D &value) 
    { doSetParmValue(idx, value); }
    void        setParmValue(exint idx, const UT_Matrix4D &value) 
    { doSetParmValue(idx, value); }
    void        setParmValue(exint idx, const UT_StringHolder &value) 
    { doSetParmValue(idx, value); }
    void        setParmValue(exint idx, const UT_SharedPtr<UT_Ramp> &value) 
    { doSetParmValue(idx, value); }
    void        setParmValue(exint idx, const PRM_DataItemHandle &value) 
    { doSetParmValue(idx, value); }

    virtual exint    getNumParms() const
    {
        return 8;
    }

    virtual const char *getParmName(exint fieldnum) const
    {
        switch (fieldnum)
        {
            case 0:
                return "group";
            case 1:
                return "iso";
            case 2:
                return "invert";
            case 3:
                return "usemaxdist";
            case 4:
                return "maxdist";
            case 5:
                return "rebuildwithfim";
            case 6:
                return "fimtolerance";
            case 7:
                return "fimiterations";

        }
        return 0;
    }

    virtual ParmType getParmType(exint fieldnum) const
    {
        switch (fieldnum)
        {
                case 0:
                    return PARM_STRING;
                case 1:
                    return PARM_FLOAT;
                case 2:
                    return PARM_INTEGER;
                case 3:
                    return PARM_INTEGER;
                case 4:
                    return PARM_FLOAT;
                case 5:
                    return PARM_INTEGER;
                case 6:
                    return PARM_FLOAT;
                case 7:
                    return PARM_INTEGER;

        }
        return PARM_UNSUPPORTED;
    }

    // Boiler plate to load individual types.
    static void  loadData(UT_IStream &is, int64 &v)
    { is.bread(&v, 1); }
    static void  loadData(UT_IStream &is, bool &v)
    { int64     iv; is.bread(&iv, 1); v = iv; }
    static void  loadData(UT_IStream &is, fpreal64 &v)
    { is.bread<fpreal64>(&v, 1); }
    static void  loadData(UT_IStream &is, UT_Vector2D &v)
    { is.bread<fpreal64>(&v.x(), 1); is.bread<fpreal64>(&v.y(), 1); }
    static void  loadData(UT_IStream &is, UT_Vector3D &v)
    { is.bread<fpreal64>(&v.x(), 1); is.bread<fpreal64>(&v.y(), 1);
      is.bread<fpreal64>(&v.z(), 1); }
    static void  loadData(UT_IStream &is, UT_Vector4D &v)
    { is.bread<fpreal64>(&v.x(), 1); is.bread<fpreal64>(&v.y(), 1);
      is.bread<fpreal64>(&v.z(), 1); is.bread<fpreal64>(&v.w(), 1); }
    static void  loadData(UT_IStream &is, UT_Matrix2D &v)
    { for (int r = 0; r < 2; r++) for (int c = 0; c < 2; c++) is.bread<fpreal64>(&v(r, c), 1); }
    static void  loadData(UT_IStream &is, UT_Matrix3D &v)
    { for (int r = 0; r < 3; r++) for (int c = 0; c < 3; c++) is.bread<fpreal64>(&v(r, c), 1); }
    static void  loadData(UT_IStream &is, UT_Matrix4D &v)
    { for (int r = 0; r < 4; r++) for (int c = 0; c < 4; c++) is.bread<fpreal64>(&v(r, c), 1); }
    static void  loadData(UT_IStream &is, UT_Vector2I &v)
    { is.bread<int64>(&v.x(), 1); is.bread<int64>(&v.y(), 1); }
    static void  loadData(UT_IStream &is, UT_Vector3I &v)
    { is.bread<int64>(&v.x(), 1); is.bread<int64>(&v.y(), 1);
      is.bread<int64>(&v.z(), 1); }
    static void  loadData(UT_IStream &is, UT_Vector4I &v)
    { is.bread<int64>(&v.x(), 1); is.bread<int64>(&v.y(), 1);
      is.bread<int64>(&v.z(), 1); is.bread<int64>(&v.w(), 1); }
    static void  loadData(UT_IStream &is, UT_StringHolder &v)
    { is.bread(v); }
    static void  loadData(UT_IStream &is, UT_SharedPtr<UT_Ramp> &v)
    {   UT_StringHolder   rampdata;
        loadData(is, rampdata);
        if (rampdata.isstring())
        {
            v.reset(new UT_Ramp());
            UT_IStream  istr((const char *) rampdata, rampdata.length(), UT_ISTREAM_ASCII);
            v->load(istr);
        }
        else v.reset();
    }
    static void  loadData(UT_IStream &is, PRM_DataItemHandle &v)
    {   UT_StringHolder   data;
        loadData(is, data);
        if (data.isstring())
        {
            // Find the data type.
            char *colon = UT_StringWrap(data).findChar(':');
            if (colon)
            {
                int             typelen = colon - data.buffer();
                UT_WorkBuffer   type;
                type.strncpy(data.buffer(), typelen);
                UT_IStream  istr(((const char *) data) + typelen + 1, data.length() - (typelen + 1), UT_ISTREAM_BINARY);
                
                v = PRM_DataFactory::parseBinary(type.buffer(), istr);
            }
        }
        else v.reset();
    }

    static void  saveData(std::ostream &os, int64 v)
    { UTwrite(os, &v); }
    static void  saveData(std::ostream &os, bool v)
    { int64 iv = v; UTwrite(os, &iv); }
    static void  saveData(std::ostream &os, fpreal64 v)
    { UTwrite<fpreal64>(os, &v); }
    static void  saveData(std::ostream &os, UT_Vector2D v)
    { UTwrite<fpreal64>(os, &v.x()); UTwrite<fpreal64>(os, &v.y()); }
    static void  saveData(std::ostream &os, UT_Vector3D v)
    { UTwrite<fpreal64>(os, &v.x()); UTwrite<fpreal64>(os, &v.y());
      UTwrite<fpreal64>(os, &v.z()); }
    static void  saveData(std::ostream &os, UT_Vector4D v)
    { UTwrite<fpreal64>(os, &v.x()); UTwrite<fpreal64>(os, &v.y());
      UTwrite<fpreal64>(os, &v.z()); UTwrite<fpreal64>(os, &v.w()); }
    static void  saveData(std::ostream &os, UT_Matrix2D v)
    { for (int r = 0; r < 2; r++) for (int c = 0; c < 2; c++) UTwrite<fpreal64>(os, &v(r, c)); }
    static void  saveData(std::ostream &os, UT_Matrix3D v)
    { for (int r = 0; r < 3; r++) for (int c = 0; c < 3; c++) UTwrite<fpreal64>(os, &v(r, c)); }
    static void  saveData(std::ostream &os, UT_Matrix4D v)
    { for (int r = 0; r < 4; r++) for (int c = 0; c < 4; c++) UTwrite<fpreal64>(os, &v(r, c)); }
    static void  saveData(std::ostream &os, UT_StringHolder s)
    { UT_StringWrap(s).saveBinary(os); }
    static void  saveData(std::ostream &os, UT_SharedPtr<UT_Ramp> s)
    {   UT_StringHolder         result;
        UT_OStringStream        ostr;
        if (s) s->save(ostr);
        result = ostr.str();
        saveData(os, result);
    }
    static void  saveData(std::ostream &os, PRM_DataItemHandle s)
    {   UT_StringHolder         result;
        UT_OStringStream        ostr;
        if (s) 
        {
            ostr << s->getDataTypeToken();
            ostr << ":";
            s->saveBinary(ostr);
        }
        result = ostr.str();
        saveData(os, result);
    }


    void         save(std::ostream &os) const
    {
        int32           v = version();
        UTwrite(os, &v);
        saveData(os, myGroup);
        saveData(os, myIso);
        saveData(os, myInvert);
        saveData(os, myUsemaxdist);
        saveData(os, myMaxdist);
        saveData(os, myRebuildwithfim);
        saveData(os, myFimtolerance);
        saveData(os, myFimiterations);

    }

    bool         load(UT_IStream &is)
    {
        int32           v;
        is.bread(&v, 1);
        if (version() != v)
        {
            // Fail incompatible versions
            return false;
        }
        loadData(is, myGroup);
        loadData(is, myIso);
        loadData(is, myInvert);
        loadData(is, myUsemaxdist);
        loadData(is, myMaxdist);
        loadData(is, myRebuildwithfim);
        loadData(is, myFimtolerance);
        loadData(is, myFimiterations);

        return true;
    }

    const UT_StringHolder & getGroup() const { return myGroup; }
    void setGroup(const UT_StringHolder & val) { myGroup = val; }
    UT_StringHolder opGroup(const SOP_NodeVerb::CookParms &cookparms) const
    { 
        SOP_Node *thissop = cookparms.getNode();
        if (!thissop) return getGroup();
        UT_StringHolder result;
        OP_Utils::evalOpParm(result, thissop, "group", cookparms.getCookTime(), 0);
        return result;
    }
    fpreal64 getIso() const { return myIso; }
    void setIso(fpreal64 val) { myIso = val; }
    fpreal64 opIso(const SOP_NodeVerb::CookParms &cookparms) const
    { 
        SOP_Node *thissop = cookparms.getNode();
        if (!thissop) return getIso();
        fpreal64 result;
        OP_Utils::evalOpParm(result, thissop, "iso", cookparms.getCookTime(), 0);
        return result;
    }
    bool getInvert() const { return myInvert; }
    void setInvert(bool val) { myInvert = val; }
    bool opInvert(const SOP_NodeVerb::CookParms &cookparms) const
    { 
        SOP_Node *thissop = cookparms.getNode();
        if (!thissop) return getInvert();
        bool result;
        OP_Utils::evalOpParm(result, thissop, "invert", cookparms.getCookTime(), 0);
        return result;
    }
    bool getUsemaxdist() const { return myUsemaxdist; }
    void setUsemaxdist(bool val) { myUsemaxdist = val; }
    bool opUsemaxdist(const SOP_NodeVerb::CookParms &cookparms) const
    { 
        SOP_Node *thissop = cookparms.getNode();
        if (!thissop) return getUsemaxdist();
        bool result;
        OP_Utils::evalOpParm(result, thissop, "usemaxdist", cookparms.getCookTime(), 0);
        return result;
    }
    fpreal64 getMaxdist() const { return myMaxdist; }
    void setMaxdist(fpreal64 val) { myMaxdist = val; }
    fpreal64 opMaxdist(const SOP_NodeVerb::CookParms &cookparms) const
    { 
        SOP_Node *thissop = cookparms.getNode();
        if (!thissop) return getMaxdist();
        fpreal64 result;
        OP_Utils::evalOpParm(result, thissop, "maxdist", cookparms.getCookTime(), 0);
        return result;
    }
    bool getRebuildwithfim() const { return myRebuildwithfim; }
    void setRebuildwithfim(bool val) { myRebuildwithfim = val; }
    bool opRebuildwithfim(const SOP_NodeVerb::CookParms &cookparms) const
    { 
        SOP_Node *thissop = cookparms.getNode();
        if (!thissop) return getRebuildwithfim();
        bool result;
        OP_Utils::evalOpParm(result, thissop, "rebuildwithfim", cookparms.getCookTime(), 0);
        return result;
    }
    fpreal64 getFimtolerance() const { return myFimtolerance; }
    void setFimtolerance(fpreal64 val) { myFimtolerance = val; }
    fpreal64 opFimtolerance(const SOP_NodeVerb::CookParms &cookparms) const
    { 
        SOP_Node *thissop = cookparms.getNode();
        if (!thissop) return getFimtolerance();
        fpreal64 result;
        OP_Utils::evalOpParm(result, thissop, "fimtolerance", cookparms.getCookTime(), 0);
        return result;
    }
    int64 getFimiterations() const { return myFimiterations; }
    void setFimiterations(int64 val) { myFimiterations = val; }
    int64 opFimiterations(const SOP_NodeVerb::CookParms &cookparms) const
    { 
        SOP_Node *thissop = cookparms.getNode();
        if (!thissop) return getFimiterations();
        int64 result;
        OP_Utils::evalOpParm(result, thissop, "fimiterations", cookparms.getCookTime(), 0);
        return result;
    }

private:
    UT_StringHolder myGroup;
    fpreal64 myIso;
    bool myInvert;
    bool myUsemaxdist;
    fpreal64 myMaxdist;
    bool myRebuildwithfim;
    fpreal64 myFimtolerance;
    int64 myFimiterations;

};
