/* Automagically Generated by generate_proto.py
 * Do not Edit
 */
#pragma once

#include <SOP/SOP_NodeVerb.h>
#include <OP/OP_Utils.h>
#include <PRM/PRM_Parm.h>
#include <UT/UT_IStream.h>
#include <UT/UT_NTStreamUtil.h>
#include <UT/UT_Ramp.h>
#include <UT/UT_SharedPtr.h>
#include <UT/UT_StringHolder.h>
#include <UT/UT_StringStream.h>
#include <UT/UT_VectorTypes.h>
#include <SYS/SYS_Types.h>

using namespace UT::Literal;

class DEP_MicroNode;
namespace SOP_ConnectivityEnums
{
    enum class Connecttype
    {
        POINT = 0,
        PRIM
    };
}


class SOP_API SOP_ConnectivityParms  : public SOP_NodeParms
{
public:
    static int version() { return 1; }

    SOP_ConnectivityParms()
    {
        myConnecttype = 0;
        myAttribname = "class"_sh;
        myCreatevarmap = false;
        myVarname = ""_sh;
        mySeamgroup = ""_sh;
        myByuv = false;
        myUVAttrib = "uv"_sh;

    }

    explicit SOP_ConnectivityParms(const SOP_ConnectivityParms &) = default;

    virtual ~SOP_ConnectivityParms() {}

    bool operator==(const SOP_ConnectivityParms &src) const
    {
        if (myConnecttype != src.myConnecttype) return false;
        if (myAttribname != src.myAttribname) return false;
        if (myCreatevarmap != src.myCreatevarmap) return false;
        if (myVarname != src.myVarname) return false;
        if (mySeamgroup != src.mySeamgroup) return false;
        if (myByuv != src.myByuv) return false;
        if (myUVAttrib != src.myUVAttrib) return false;

        return true;
    }
    bool operator!=(const SOP_ConnectivityParms &src) const
    {
        return !operator==(src);
    }
    using Connecttype = SOP_ConnectivityEnums::Connecttype;



    void        buildFromOp(const OP_Node *node, fpreal time, DEP_MicroNode *depnode)
    {
        if (true)
            OP_Utils::evalOpParm(myConnecttype, node, "connecttype", time, 0);
        else myConnecttype = 0;
        if (true)
            OP_Utils::evalOpParm(myAttribname, node, "attribname", time, 0);
        else myAttribname = "class"_sh;
        if (true)
            OP_Utils::evalOpParm(myCreatevarmap, node, "createvarmap", time, 0);
        else myCreatevarmap = false;
        if (true && ( (!(((getCreatevarmap()==0)))) ) )
            OP_Utils::evalOpParm(myVarname, node, "varname", time, 0);
        else myVarname = ""_sh;
        if (true && ( (!(((int64(getConnecttype())==0)))) ) )
            OP_Utils::evalOpParm(mySeamgroup, node, "seamgroup", time, 0);
        else mySeamgroup = ""_sh;
        if (true && ( (!(((int64(getConnecttype())==0))||((getSeamgroup()!="")))) ) )
            OP_Utils::evalOpParm(myByuv, node, "byuv", time, 0);
        else myByuv = false;
        if (true && ( (!(((int64(getConnecttype())==0))||((getSeamgroup()!=""))||((getByuv()==0)))) ) )
            OP_Utils::evalOpParm(myUVAttrib, node, "uvattrib", time, 0);
        else myUVAttrib = "uv"_sh;

    }


    virtual void loadFromOpSubclass(const LoadParms &loadparms)
    {
        buildFromOp(loadparms.node(), loadparms.context().getTime(), loadparms.depnode());
    }


    virtual void copyFrom(const SOP_NodeParms *src)
    {
        *this = *((const SOP_ConnectivityParms *)src);
    }

    template <typename T>
    void
    doGetParmValue(exint idx, T &value) const
    {
        switch (idx)
        {
            case 0:
                coerceValue(value, myConnecttype);
                break;
            case 1:
                coerceValue(value, myAttribname);
                break;
            case 2:
                coerceValue(value, myCreatevarmap);
                break;
            case 3:
                coerceValue(value, myVarname);
                break;
            case 4:
                coerceValue(value, mySeamgroup);
                break;
            case 5:
                coerceValue(value, myByuv);
                break;
            case 6:
                coerceValue(value, myUVAttrib);
                break;

        }
    }

    void        getParmValue(exint idx, exint &value) const
    { doGetParmValue(idx, value); }
    void        getParmValue(exint idx, fpreal &value) const
    { doGetParmValue(idx, value); }
    void        getParmValue(exint idx, UT_Vector2D &value) const
    { doGetParmValue(idx, value); }
    void        getParmValue(exint idx, UT_Vector3D &value) const
    { doGetParmValue(idx, value); }
    void        getParmValue(exint idx, UT_Vector4D &value) const
    { doGetParmValue(idx, value); }
    void        getParmValue(exint idx, UT_Matrix2D &value) const
    { doGetParmValue(idx, value); }
    void        getParmValue(exint idx, UT_Matrix3D &value) const
    { doGetParmValue(idx, value); }
    void        getParmValue(exint idx, UT_Matrix4D &value) const
    { doGetParmValue(idx, value); }
    void        getParmValue(exint idx, UT_StringHolder &value) const
    { doGetParmValue(idx, value); }
    void        getParmValue(exint idx, UT_SharedPtr<UT_Ramp> &value) const
    { doGetParmValue(idx, value); }
    void        getParmValue(exint idx, PRM_DataItemHandle &value) const
    { doGetParmValue(idx, value); }

    template <typename T>
    void
    doSetParmValue(exint idx, const T &value) 
    {
        switch (idx)
        {
            case 0:
                coerceValue(myConnecttype, value);
                break;
            case 1:
                coerceValue(myAttribname, value);
                break;
            case 2:
                coerceValue(myCreatevarmap, value);
                break;
            case 3:
                coerceValue(myVarname, value);
                break;
            case 4:
                coerceValue(mySeamgroup, value);
                break;
            case 5:
                coerceValue(myByuv, value);
                break;
            case 6:
                coerceValue(myUVAttrib, value);
                break;

        }
    }

    void        setParmValue(exint idx, const exint &value) 
    { doSetParmValue(idx, value); }
    void        setParmValue(exint idx, const fpreal &value) 
    { doSetParmValue(idx, value); }
    void        setParmValue(exint idx, const UT_Vector2D &value) 
    { doSetParmValue(idx, value); }
    void        setParmValue(exint idx, const UT_Vector3D &value) 
    { doSetParmValue(idx, value); }
    void        setParmValue(exint idx, const UT_Vector4D &value) 
    { doSetParmValue(idx, value); }
    void        setParmValue(exint idx, const UT_Matrix2D &value) 
    { doSetParmValue(idx, value); }
    void        setParmValue(exint idx, const UT_Matrix3D &value) 
    { doSetParmValue(idx, value); }
    void        setParmValue(exint idx, const UT_Matrix4D &value) 
    { doSetParmValue(idx, value); }
    void        setParmValue(exint idx, const UT_StringHolder &value) 
    { doSetParmValue(idx, value); }
    void        setParmValue(exint idx, const UT_SharedPtr<UT_Ramp> &value) 
    { doSetParmValue(idx, value); }
    void        setParmValue(exint idx, const PRM_DataItemHandle &value) 
    { doSetParmValue(idx, value); }

    virtual exint    getNumParms() const
    {
        return 7;
    }

    virtual const char *getParmName(exint fieldnum) const
    {
        switch (fieldnum)
        {
            case 0:
                return "connecttype";
            case 1:
                return "attribname";
            case 2:
                return "createvarmap";
            case 3:
                return "varname";
            case 4:
                return "seamgroup";
            case 5:
                return "byuv";
            case 6:
                return "uvattrib";

        }
        return 0;
    }

    virtual ParmType getParmType(exint fieldnum) const
    {
        switch (fieldnum)
        {
                case 0:
                    return PARM_INTEGER;
                case 1:
                    return PARM_STRING;
                case 2:
                    return PARM_INTEGER;
                case 3:
                    return PARM_STRING;
                case 4:
                    return PARM_STRING;
                case 5:
                    return PARM_INTEGER;
                case 6:
                    return PARM_STRING;

        }
        return PARM_UNSUPPORTED;
    }

    // Boiler plate to load individual types.
    static void  loadData(UT_IStream &is, int64 &v)
    { is.bread(&v, 1); }
    static void  loadData(UT_IStream &is, bool &v)
    { int64     iv; is.bread(&iv, 1); v = iv; }
    static void  loadData(UT_IStream &is, fpreal64 &v)
    { is.bread<fpreal64>(&v, 1); }
    static void  loadData(UT_IStream &is, UT_Vector2D &v)
    { is.bread<fpreal64>(&v.x(), 1); is.bread<fpreal64>(&v.y(), 1); }
    static void  loadData(UT_IStream &is, UT_Vector3D &v)
    { is.bread<fpreal64>(&v.x(), 1); is.bread<fpreal64>(&v.y(), 1);
      is.bread<fpreal64>(&v.z(), 1); }
    static void  loadData(UT_IStream &is, UT_Vector4D &v)
    { is.bread<fpreal64>(&v.x(), 1); is.bread<fpreal64>(&v.y(), 1);
      is.bread<fpreal64>(&v.z(), 1); is.bread<fpreal64>(&v.w(), 1); }
    static void  loadData(UT_IStream &is, UT_Matrix2D &v)
    { for (int r = 0; r < 2; r++) for (int c = 0; c < 2; c++) is.bread<fpreal64>(&v(r, c), 1); }
    static void  loadData(UT_IStream &is, UT_Matrix3D &v)
    { for (int r = 0; r < 3; r++) for (int c = 0; c < 3; c++) is.bread<fpreal64>(&v(r, c), 1); }
    static void  loadData(UT_IStream &is, UT_Matrix4D &v)
    { for (int r = 0; r < 4; r++) for (int c = 0; c < 4; c++) is.bread<fpreal64>(&v(r, c), 1); }
    static void  loadData(UT_IStream &is, UT_Vector2I &v)
    { is.bread<int64>(&v.x(), 1); is.bread<int64>(&v.y(), 1); }
    static void  loadData(UT_IStream &is, UT_Vector3I &v)
    { is.bread<int64>(&v.x(), 1); is.bread<int64>(&v.y(), 1);
      is.bread<int64>(&v.z(), 1); }
    static void  loadData(UT_IStream &is, UT_Vector4I &v)
    { is.bread<int64>(&v.x(), 1); is.bread<int64>(&v.y(), 1);
      is.bread<int64>(&v.z(), 1); is.bread<int64>(&v.w(), 1); }
    static void  loadData(UT_IStream &is, UT_StringHolder &v)
    { is.bread(v); }
    static void  loadData(UT_IStream &is, UT_SharedPtr<UT_Ramp> &v)
    {   UT_StringHolder   rampdata;
        loadData(is, rampdata);
        if (rampdata.isstring())
        {
            v.reset(new UT_Ramp());
            UT_IStream  istr((const char *) rampdata, rampdata.length(), UT_ISTREAM_ASCII);
            v->load(istr);
        }
        else v.reset();
    }
    static void  loadData(UT_IStream &is, PRM_DataItemHandle &v)
    {   UT_StringHolder   data;
        loadData(is, data);
        if (data.isstring())
        {
            // Find the data type.
            char *colon = UT_StringWrap(data).findChar(':');
            if (colon)
            {
                int             typelen = colon - data.buffer();
                UT_WorkBuffer   type;
                type.strncpy(data.buffer(), typelen);
                UT_IStream  istr(((const char *) data) + typelen + 1, data.length() - (typelen + 1), UT_ISTREAM_BINARY);
                
                v = PRM_DataFactory::parseBinary(type.buffer(), istr);
            }
        }
        else v.reset();
    }

    static void  saveData(std::ostream &os, int64 v)
    { UTwrite(os, &v); }
    static void  saveData(std::ostream &os, bool v)
    { int64 iv = v; UTwrite(os, &iv); }
    static void  saveData(std::ostream &os, fpreal64 v)
    { UTwrite<fpreal64>(os, &v); }
    static void  saveData(std::ostream &os, UT_Vector2D v)
    { UTwrite<fpreal64>(os, &v.x()); UTwrite<fpreal64>(os, &v.y()); }
    static void  saveData(std::ostream &os, UT_Vector3D v)
    { UTwrite<fpreal64>(os, &v.x()); UTwrite<fpreal64>(os, &v.y());
      UTwrite<fpreal64>(os, &v.z()); }
    static void  saveData(std::ostream &os, UT_Vector4D v)
    { UTwrite<fpreal64>(os, &v.x()); UTwrite<fpreal64>(os, &v.y());
      UTwrite<fpreal64>(os, &v.z()); UTwrite<fpreal64>(os, &v.w()); }
    static void  saveData(std::ostream &os, UT_Matrix2D v)
    { for (int r = 0; r < 2; r++) for (int c = 0; c < 2; c++) UTwrite<fpreal64>(os, &v(r, c)); }
    static void  saveData(std::ostream &os, UT_Matrix3D v)
    { for (int r = 0; r < 3; r++) for (int c = 0; c < 3; c++) UTwrite<fpreal64>(os, &v(r, c)); }
    static void  saveData(std::ostream &os, UT_Matrix4D v)
    { for (int r = 0; r < 4; r++) for (int c = 0; c < 4; c++) UTwrite<fpreal64>(os, &v(r, c)); }
    static void  saveData(std::ostream &os, UT_StringHolder s)
    { UT_StringWrap(s).saveBinary(os); }
    static void  saveData(std::ostream &os, UT_SharedPtr<UT_Ramp> s)
    {   UT_StringHolder         result;
        UT_OStringStream        ostr;
        if (s) s->save(ostr);
        result = ostr.str();
        saveData(os, result);
    }
    static void  saveData(std::ostream &os, PRM_DataItemHandle s)
    {   UT_StringHolder         result;
        UT_OStringStream        ostr;
        if (s) 
        {
            ostr << s->getDataTypeToken();
            ostr << ":";
            s->saveBinary(ostr);
        }
        result = ostr.str();
        saveData(os, result);
    }


    void         save(std::ostream &os) const
    {
        int32           v = version();
        UTwrite(os, &v);
        saveData(os, myConnecttype);
        saveData(os, myAttribname);
        saveData(os, myCreatevarmap);
        saveData(os, myVarname);
        saveData(os, mySeamgroup);
        saveData(os, myByuv);
        saveData(os, myUVAttrib);

    }

    bool         load(UT_IStream &is)
    {
        int32           v;
        is.bread(&v, 1);
        if (version() != v)
        {
            // Fail incompatible versions
            return false;
        }
        loadData(is, myConnecttype);
        loadData(is, myAttribname);
        loadData(is, myCreatevarmap);
        loadData(is, myVarname);
        loadData(is, mySeamgroup);
        loadData(is, myByuv);
        loadData(is, myUVAttrib);

        return true;
    }

    Connecttype getConnecttype() const { return Connecttype(myConnecttype); }
    void setConnecttype(Connecttype val) { myConnecttype = int64(val); }
    Connecttype opConnecttype(const SOP_NodeVerb::CookParms &cookparms) const
    { 
        SOP_Node *thissop = cookparms.getNode();
        if (!thissop) return getConnecttype();
        int64 result;
        OP_Utils::evalOpParm(result, thissop, "connecttype", cookparms.getCookTime(), 0);
        return Connecttype(result);
    }
    const UT_StringHolder & getAttribname() const { return myAttribname; }
    void setAttribname(const UT_StringHolder & val) { myAttribname = val; }
    UT_StringHolder opAttribname(const SOP_NodeVerb::CookParms &cookparms) const
    { 
        SOP_Node *thissop = cookparms.getNode();
        if (!thissop) return getAttribname();
        UT_StringHolder result;
        OP_Utils::evalOpParm(result, thissop, "attribname", cookparms.getCookTime(), 0);
        return result;
    }
    bool getCreatevarmap() const { return myCreatevarmap; }
    void setCreatevarmap(bool val) { myCreatevarmap = val; }
    bool opCreatevarmap(const SOP_NodeVerb::CookParms &cookparms) const
    { 
        SOP_Node *thissop = cookparms.getNode();
        if (!thissop) return getCreatevarmap();
        bool result;
        OP_Utils::evalOpParm(result, thissop, "createvarmap", cookparms.getCookTime(), 0);
        return result;
    }
    const UT_StringHolder & getVarname() const { return myVarname; }
    void setVarname(const UT_StringHolder & val) { myVarname = val; }
    UT_StringHolder opVarname(const SOP_NodeVerb::CookParms &cookparms) const
    { 
        SOP_Node *thissop = cookparms.getNode();
        if (!thissop) return getVarname();
        UT_StringHolder result;
        OP_Utils::evalOpParm(result, thissop, "varname", cookparms.getCookTime(), 0);
        return result;
    }
    const UT_StringHolder & getSeamgroup() const { return mySeamgroup; }
    void setSeamgroup(const UT_StringHolder & val) { mySeamgroup = val; }
    UT_StringHolder opSeamgroup(const SOP_NodeVerb::CookParms &cookparms) const
    { 
        SOP_Node *thissop = cookparms.getNode();
        if (!thissop) return getSeamgroup();
        UT_StringHolder result;
        OP_Utils::evalOpParm(result, thissop, "seamgroup", cookparms.getCookTime(), 0);
        return result;
    }
    bool getByuv() const { return myByuv; }
    void setByuv(bool val) { myByuv = val; }
    bool opByuv(const SOP_NodeVerb::CookParms &cookparms) const
    { 
        SOP_Node *thissop = cookparms.getNode();
        if (!thissop) return getByuv();
        bool result;
        OP_Utils::evalOpParm(result, thissop, "byuv", cookparms.getCookTime(), 0);
        return result;
    }
    const UT_StringHolder & getUVAttrib() const { return myUVAttrib; }
    void setUVAttrib(const UT_StringHolder & val) { myUVAttrib = val; }
    UT_StringHolder opUVAttrib(const SOP_NodeVerb::CookParms &cookparms) const
    { 
        SOP_Node *thissop = cookparms.getNode();
        if (!thissop) return getUVAttrib();
        UT_StringHolder result;
        OP_Utils::evalOpParm(result, thissop, "uvattrib", cookparms.getCookTime(), 0);
        return result;
    }

private:
    int64 myConnecttype;
    UT_StringHolder myAttribname;
    bool myCreatevarmap;
    UT_StringHolder myVarname;
    UT_StringHolder mySeamgroup;
    bool myByuv;
    UT_StringHolder myUVAttrib;

};
