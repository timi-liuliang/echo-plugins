/* Automagically Generated by generate_proto.py
 * Do not Edit
 */
#pragma once

#include <SOP/SOP_NodeVerb.h>
#include <OP/OP_Utils.h>
#include <PRM/PRM_Parm.h>
#include <UT/UT_IStream.h>
#include <UT/UT_NTStreamUtil.h>
#include <UT/UT_Ramp.h>
#include <UT/UT_SharedPtr.h>
#include <UT/UT_StringHolder.h>
#include <UT/UT_StringStream.h>
#include <UT/UT_VectorTypes.h>
#include <SYS/SYS_Types.h>

using namespace UT::Literal;

class DEP_MicroNode;

class SOP_API SOP_IntersectionAnalysisParms  : public SOP_NodeParms
{
public:
    static int version() { return 1; }

    SOP_IntersectionAnalysisParms()
    {
        myAgroup = ""_sh;
        myBgroup = ""_sh;
        myUseproxtol = true;
        myProxtol = 0.01;
        myDetectverts = false;
        myOutputsegs = false;
        myUseinputnumattrib = false;
        myInputnumattrib = "sourceinput"_sh;
        myUseprimnumattrib = false;
        myPrimnumattrib = "sourceprim"_sh;
        myUseprimuvwattrib = false;
        myPrimuvwattrib = "sourceprimuv"_sh;
        myUseptnumattrib = false;
        myPtnumattrib = "sourceptnum"_sh;

    }

    explicit SOP_IntersectionAnalysisParms(const SOP_IntersectionAnalysisParms &) = default;

    virtual ~SOP_IntersectionAnalysisParms() {}

    bool operator==(const SOP_IntersectionAnalysisParms &src) const
    {
        if (myAgroup != src.myAgroup) return false;
        if (myBgroup != src.myBgroup) return false;
        if (myUseproxtol != src.myUseproxtol) return false;
        if (myProxtol != src.myProxtol) return false;
        if (myDetectverts != src.myDetectverts) return false;
        if (myOutputsegs != src.myOutputsegs) return false;
        if (myUseinputnumattrib != src.myUseinputnumattrib) return false;
        if (myInputnumattrib != src.myInputnumattrib) return false;
        if (myUseprimnumattrib != src.myUseprimnumattrib) return false;
        if (myPrimnumattrib != src.myPrimnumattrib) return false;
        if (myUseprimuvwattrib != src.myUseprimuvwattrib) return false;
        if (myPrimuvwattrib != src.myPrimuvwattrib) return false;
        if (myUseptnumattrib != src.myUseptnumattrib) return false;
        if (myPtnumattrib != src.myPtnumattrib) return false;

        return true;
    }
    bool operator!=(const SOP_IntersectionAnalysisParms &src) const
    {
        return !operator==(src);
    }



    void        buildFromOp(const OP_Node *node, fpreal time, DEP_MicroNode *depnode)
    {
        if (true)
            OP_Utils::evalOpParm(myAgroup, node, "agroup", time, 0);
        else myAgroup = ""_sh;
        if (true)
            OP_Utils::evalOpParm(myBgroup, node, "bgroup", time, 0);
        else myBgroup = ""_sh;
        if (true)
            OP_Utils::evalOpParm(myUseproxtol, node, "useproxtol", time, 0);
        else myUseproxtol = true;
        if (true && ( (!(((getUseproxtol()==0)))) ) )
            OP_Utils::evalOpParm(myProxtol, node, "proxtol", time, 0);
        else myProxtol = 0.01;
        if (true)
            OP_Utils::evalOpParm(myDetectverts, node, "detectverts", time, 0);
        else myDetectverts = false;
        if (true)
            OP_Utils::evalOpParm(myOutputsegs, node, "outputsegs", time, 0);
        else myOutputsegs = false;
        if (true)
            OP_Utils::evalOpParm(myUseinputnumattrib, node, "useinputnumattrib", time, 0);
        else myUseinputnumattrib = false;
        if (true && ( (!(((getUseinputnumattrib()==0)))) ) )
            OP_Utils::evalOpParm(myInputnumattrib, node, "inputnumattrib", time, 0);
        else myInputnumattrib = "sourceinput"_sh;
        if (true)
            OP_Utils::evalOpParm(myUseprimnumattrib, node, "useprimnumattrib", time, 0);
        else myUseprimnumattrib = false;
        if (true && ( (!(((getUseprimnumattrib()==0)))) ) )
            OP_Utils::evalOpParm(myPrimnumattrib, node, "primnumattrib", time, 0);
        else myPrimnumattrib = "sourceprim"_sh;
        if (true)
            OP_Utils::evalOpParm(myUseprimuvwattrib, node, "useprimuvwattrib", time, 0);
        else myUseprimuvwattrib = false;
        if (true && ( (!(((getUseprimuvwattrib()==0)))) ) )
            OP_Utils::evalOpParm(myPrimuvwattrib, node, "primuvwattrib", time, 0);
        else myPrimuvwattrib = "sourceprimuv"_sh;
        if (true)
            OP_Utils::evalOpParm(myUseptnumattrib, node, "useptnumattrib", time, 0);
        else myUseptnumattrib = false;
        if (true && ( (!(((getUseptnumattrib()==0)))) ) )
            OP_Utils::evalOpParm(myPtnumattrib, node, "ptnumattrib", time, 0);
        else myPtnumattrib = "sourceptnum"_sh;

    }


    virtual void loadFromOpSubclass(const LoadParms &loadparms)
    {
        buildFromOp(loadparms.node(), loadparms.context().getTime(), loadparms.depnode());
    }


    virtual void copyFrom(const SOP_NodeParms *src)
    {
        *this = *((const SOP_IntersectionAnalysisParms *)src);
    }

    template <typename T>
    void
    doGetParmValue(exint idx, T &value) const
    {
        switch (idx)
        {
            case 0:
                coerceValue(value, myAgroup);
                break;
            case 1:
                coerceValue(value, myBgroup);
                break;
            case 2:
                coerceValue(value, myUseproxtol);
                break;
            case 3:
                coerceValue(value, myProxtol);
                break;
            case 4:
                coerceValue(value, myDetectverts);
                break;
            case 5:
                coerceValue(value, myOutputsegs);
                break;
            case 6:
                coerceValue(value, myUseinputnumattrib);
                break;
            case 7:
                coerceValue(value, myInputnumattrib);
                break;
            case 8:
                coerceValue(value, myUseprimnumattrib);
                break;
            case 9:
                coerceValue(value, myPrimnumattrib);
                break;
            case 10:
                coerceValue(value, myUseprimuvwattrib);
                break;
            case 11:
                coerceValue(value, myPrimuvwattrib);
                break;
            case 12:
                coerceValue(value, myUseptnumattrib);
                break;
            case 13:
                coerceValue(value, myPtnumattrib);
                break;

        }
    }

    void        getParmValue(exint idx, exint &value) const
    { doGetParmValue(idx, value); }
    void        getParmValue(exint idx, fpreal &value) const
    { doGetParmValue(idx, value); }
    void        getParmValue(exint idx, UT_Vector2D &value) const
    { doGetParmValue(idx, value); }
    void        getParmValue(exint idx, UT_Vector3D &value) const
    { doGetParmValue(idx, value); }
    void        getParmValue(exint idx, UT_Vector4D &value) const
    { doGetParmValue(idx, value); }
    void        getParmValue(exint idx, UT_Matrix2D &value) const
    { doGetParmValue(idx, value); }
    void        getParmValue(exint idx, UT_Matrix3D &value) const
    { doGetParmValue(idx, value); }
    void        getParmValue(exint idx, UT_Matrix4D &value) const
    { doGetParmValue(idx, value); }
    void        getParmValue(exint idx, UT_StringHolder &value) const
    { doGetParmValue(idx, value); }
    void        getParmValue(exint idx, UT_SharedPtr<UT_Ramp> &value) const
    { doGetParmValue(idx, value); }
    void        getParmValue(exint idx, PRM_DataItemHandle &value) const
    { doGetParmValue(idx, value); }

    template <typename T>
    void
    doSetParmValue(exint idx, const T &value) 
    {
        switch (idx)
        {
            case 0:
                coerceValue(myAgroup, value);
                break;
            case 1:
                coerceValue(myBgroup, value);
                break;
            case 2:
                coerceValue(myUseproxtol, value);
                break;
            case 3:
                coerceValue(myProxtol, value);
                break;
            case 4:
                coerceValue(myDetectverts, value);
                break;
            case 5:
                coerceValue(myOutputsegs, value);
                break;
            case 6:
                coerceValue(myUseinputnumattrib, value);
                break;
            case 7:
                coerceValue(myInputnumattrib, value);
                break;
            case 8:
                coerceValue(myUseprimnumattrib, value);
                break;
            case 9:
                coerceValue(myPrimnumattrib, value);
                break;
            case 10:
                coerceValue(myUseprimuvwattrib, value);
                break;
            case 11:
                coerceValue(myPrimuvwattrib, value);
                break;
            case 12:
                coerceValue(myUseptnumattrib, value);
                break;
            case 13:
                coerceValue(myPtnumattrib, value);
                break;

        }
    }

    void        setParmValue(exint idx, const exint &value) 
    { doSetParmValue(idx, value); }
    void        setParmValue(exint idx, const fpreal &value) 
    { doSetParmValue(idx, value); }
    void        setParmValue(exint idx, const UT_Vector2D &value) 
    { doSetParmValue(idx, value); }
    void        setParmValue(exint idx, const UT_Vector3D &value) 
    { doSetParmValue(idx, value); }
    void        setParmValue(exint idx, const UT_Vector4D &value) 
    { doSetParmValue(idx, value); }
    void        setParmValue(exint idx, const UT_Matrix2D &value) 
    { doSetParmValue(idx, value); }
    void        setParmValue(exint idx, const UT_Matrix3D &value) 
    { doSetParmValue(idx, value); }
    void        setParmValue(exint idx, const UT_Matrix4D &value) 
    { doSetParmValue(idx, value); }
    void        setParmValue(exint idx, const UT_StringHolder &value) 
    { doSetParmValue(idx, value); }
    void        setParmValue(exint idx, const UT_SharedPtr<UT_Ramp> &value) 
    { doSetParmValue(idx, value); }
    void        setParmValue(exint idx, const PRM_DataItemHandle &value) 
    { doSetParmValue(idx, value); }

    virtual exint    getNumParms() const
    {
        return 14;
    }

    virtual const char *getParmName(exint fieldnum) const
    {
        switch (fieldnum)
        {
            case 0:
                return "agroup";
            case 1:
                return "bgroup";
            case 2:
                return "useproxtol";
            case 3:
                return "proxtol";
            case 4:
                return "detectverts";
            case 5:
                return "outputsegs";
            case 6:
                return "useinputnumattrib";
            case 7:
                return "inputnumattrib";
            case 8:
                return "useprimnumattrib";
            case 9:
                return "primnumattrib";
            case 10:
                return "useprimuvwattrib";
            case 11:
                return "primuvwattrib";
            case 12:
                return "useptnumattrib";
            case 13:
                return "ptnumattrib";

        }
        return 0;
    }

    virtual ParmType getParmType(exint fieldnum) const
    {
        switch (fieldnum)
        {
                case 0:
                    return PARM_STRING;
                case 1:
                    return PARM_STRING;
                case 2:
                    return PARM_INTEGER;
                case 3:
                    return PARM_FLOAT;
                case 4:
                    return PARM_INTEGER;
                case 5:
                    return PARM_INTEGER;
                case 6:
                    return PARM_INTEGER;
                case 7:
                    return PARM_STRING;
                case 8:
                    return PARM_INTEGER;
                case 9:
                    return PARM_STRING;
                case 10:
                    return PARM_INTEGER;
                case 11:
                    return PARM_STRING;
                case 12:
                    return PARM_INTEGER;
                case 13:
                    return PARM_STRING;

        }
        return PARM_UNSUPPORTED;
    }

    // Boiler plate to load individual types.
    static void  loadData(UT_IStream &is, int64 &v)
    { is.bread(&v, 1); }
    static void  loadData(UT_IStream &is, bool &v)
    { int64     iv; is.bread(&iv, 1); v = iv; }
    static void  loadData(UT_IStream &is, fpreal64 &v)
    { is.bread<fpreal64>(&v, 1); }
    static void  loadData(UT_IStream &is, UT_Vector2D &v)
    { is.bread<fpreal64>(&v.x(), 1); is.bread<fpreal64>(&v.y(), 1); }
    static void  loadData(UT_IStream &is, UT_Vector3D &v)
    { is.bread<fpreal64>(&v.x(), 1); is.bread<fpreal64>(&v.y(), 1);
      is.bread<fpreal64>(&v.z(), 1); }
    static void  loadData(UT_IStream &is, UT_Vector4D &v)
    { is.bread<fpreal64>(&v.x(), 1); is.bread<fpreal64>(&v.y(), 1);
      is.bread<fpreal64>(&v.z(), 1); is.bread<fpreal64>(&v.w(), 1); }
    static void  loadData(UT_IStream &is, UT_Matrix2D &v)
    { for (int r = 0; r < 2; r++) for (int c = 0; c < 2; c++) is.bread<fpreal64>(&v(r, c), 1); }
    static void  loadData(UT_IStream &is, UT_Matrix3D &v)
    { for (int r = 0; r < 3; r++) for (int c = 0; c < 3; c++) is.bread<fpreal64>(&v(r, c), 1); }
    static void  loadData(UT_IStream &is, UT_Matrix4D &v)
    { for (int r = 0; r < 4; r++) for (int c = 0; c < 4; c++) is.bread<fpreal64>(&v(r, c), 1); }
    static void  loadData(UT_IStream &is, UT_Vector2I &v)
    { is.bread<int64>(&v.x(), 1); is.bread<int64>(&v.y(), 1); }
    static void  loadData(UT_IStream &is, UT_Vector3I &v)
    { is.bread<int64>(&v.x(), 1); is.bread<int64>(&v.y(), 1);
      is.bread<int64>(&v.z(), 1); }
    static void  loadData(UT_IStream &is, UT_Vector4I &v)
    { is.bread<int64>(&v.x(), 1); is.bread<int64>(&v.y(), 1);
      is.bread<int64>(&v.z(), 1); is.bread<int64>(&v.w(), 1); }
    static void  loadData(UT_IStream &is, UT_StringHolder &v)
    { is.bread(v); }
    static void  loadData(UT_IStream &is, UT_SharedPtr<UT_Ramp> &v)
    {   UT_StringHolder   rampdata;
        loadData(is, rampdata);
        if (rampdata.isstring())
        {
            v.reset(new UT_Ramp());
            UT_IStream  istr((const char *) rampdata, rampdata.length(), UT_ISTREAM_ASCII);
            v->load(istr);
        }
        else v.reset();
    }
    static void  loadData(UT_IStream &is, PRM_DataItemHandle &v)
    {   UT_StringHolder   data;
        loadData(is, data);
        if (data.isstring())
        {
            // Find the data type.
            char *colon = UT_StringWrap(data).findChar(':');
            if (colon)
            {
                int             typelen = colon - data.buffer();
                UT_WorkBuffer   type;
                type.strncpy(data.buffer(), typelen);
                UT_IStream  istr(((const char *) data) + typelen + 1, data.length() - (typelen + 1), UT_ISTREAM_BINARY);
                
                v = PRM_DataFactory::parseBinary(type.buffer(), istr);
            }
        }
        else v.reset();
    }

    static void  saveData(std::ostream &os, int64 v)
    { UTwrite(os, &v); }
    static void  saveData(std::ostream &os, bool v)
    { int64 iv = v; UTwrite(os, &iv); }
    static void  saveData(std::ostream &os, fpreal64 v)
    { UTwrite<fpreal64>(os, &v); }
    static void  saveData(std::ostream &os, UT_Vector2D v)
    { UTwrite<fpreal64>(os, &v.x()); UTwrite<fpreal64>(os, &v.y()); }
    static void  saveData(std::ostream &os, UT_Vector3D v)
    { UTwrite<fpreal64>(os, &v.x()); UTwrite<fpreal64>(os, &v.y());
      UTwrite<fpreal64>(os, &v.z()); }
    static void  saveData(std::ostream &os, UT_Vector4D v)
    { UTwrite<fpreal64>(os, &v.x()); UTwrite<fpreal64>(os, &v.y());
      UTwrite<fpreal64>(os, &v.z()); UTwrite<fpreal64>(os, &v.w()); }
    static void  saveData(std::ostream &os, UT_Matrix2D v)
    { for (int r = 0; r < 2; r++) for (int c = 0; c < 2; c++) UTwrite<fpreal64>(os, &v(r, c)); }
    static void  saveData(std::ostream &os, UT_Matrix3D v)
    { for (int r = 0; r < 3; r++) for (int c = 0; c < 3; c++) UTwrite<fpreal64>(os, &v(r, c)); }
    static void  saveData(std::ostream &os, UT_Matrix4D v)
    { for (int r = 0; r < 4; r++) for (int c = 0; c < 4; c++) UTwrite<fpreal64>(os, &v(r, c)); }
    static void  saveData(std::ostream &os, UT_StringHolder s)
    { UT_StringWrap(s).saveBinary(os); }
    static void  saveData(std::ostream &os, UT_SharedPtr<UT_Ramp> s)
    {   UT_StringHolder         result;
        UT_OStringStream        ostr;
        if (s) s->save(ostr);
        result = ostr.str();
        saveData(os, result);
    }
    static void  saveData(std::ostream &os, PRM_DataItemHandle s)
    {   UT_StringHolder         result;
        UT_OStringStream        ostr;
        if (s) 
        {
            ostr << s->getDataTypeToken();
            ostr << ":";
            s->saveBinary(ostr);
        }
        result = ostr.str();
        saveData(os, result);
    }


    void         save(std::ostream &os) const
    {
        int32           v = version();
        UTwrite(os, &v);
        saveData(os, myAgroup);
        saveData(os, myBgroup);
        saveData(os, myUseproxtol);
        saveData(os, myProxtol);
        saveData(os, myDetectverts);
        saveData(os, myOutputsegs);
        saveData(os, myUseinputnumattrib);
        saveData(os, myInputnumattrib);
        saveData(os, myUseprimnumattrib);
        saveData(os, myPrimnumattrib);
        saveData(os, myUseprimuvwattrib);
        saveData(os, myPrimuvwattrib);
        saveData(os, myUseptnumattrib);
        saveData(os, myPtnumattrib);

    }

    bool         load(UT_IStream &is)
    {
        int32           v;
        is.bread(&v, 1);
        if (version() != v)
        {
            // Fail incompatible versions
            return false;
        }
        loadData(is, myAgroup);
        loadData(is, myBgroup);
        loadData(is, myUseproxtol);
        loadData(is, myProxtol);
        loadData(is, myDetectverts);
        loadData(is, myOutputsegs);
        loadData(is, myUseinputnumattrib);
        loadData(is, myInputnumattrib);
        loadData(is, myUseprimnumattrib);
        loadData(is, myPrimnumattrib);
        loadData(is, myUseprimuvwattrib);
        loadData(is, myPrimuvwattrib);
        loadData(is, myUseptnumattrib);
        loadData(is, myPtnumattrib);

        return true;
    }

    const UT_StringHolder & getAgroup() const { return myAgroup; }
    void setAgroup(const UT_StringHolder & val) { myAgroup = val; }
    UT_StringHolder opAgroup(const SOP_NodeVerb::CookParms &cookparms) const
    { 
        SOP_Node *thissop = cookparms.getNode();
        if (!thissop) return getAgroup();
        UT_StringHolder result;
        OP_Utils::evalOpParm(result, thissop, "agroup", cookparms.getCookTime(), 0);
        return result;
    }
    const UT_StringHolder & getBgroup() const { return myBgroup; }
    void setBgroup(const UT_StringHolder & val) { myBgroup = val; }
    UT_StringHolder opBgroup(const SOP_NodeVerb::CookParms &cookparms) const
    { 
        SOP_Node *thissop = cookparms.getNode();
        if (!thissop) return getBgroup();
        UT_StringHolder result;
        OP_Utils::evalOpParm(result, thissop, "bgroup", cookparms.getCookTime(), 0);
        return result;
    }
    bool getUseproxtol() const { return myUseproxtol; }
    void setUseproxtol(bool val) { myUseproxtol = val; }
    bool opUseproxtol(const SOP_NodeVerb::CookParms &cookparms) const
    { 
        SOP_Node *thissop = cookparms.getNode();
        if (!thissop) return getUseproxtol();
        bool result;
        OP_Utils::evalOpParm(result, thissop, "useproxtol", cookparms.getCookTime(), 0);
        return result;
    }
    fpreal64 getProxtol() const { return myProxtol; }
    void setProxtol(fpreal64 val) { myProxtol = val; }
    fpreal64 opProxtol(const SOP_NodeVerb::CookParms &cookparms) const
    { 
        SOP_Node *thissop = cookparms.getNode();
        if (!thissop) return getProxtol();
        fpreal64 result;
        OP_Utils::evalOpParm(result, thissop, "proxtol", cookparms.getCookTime(), 0);
        return result;
    }
    bool getDetectverts() const { return myDetectverts; }
    void setDetectverts(bool val) { myDetectverts = val; }
    bool opDetectverts(const SOP_NodeVerb::CookParms &cookparms) const
    { 
        SOP_Node *thissop = cookparms.getNode();
        if (!thissop) return getDetectverts();
        bool result;
        OP_Utils::evalOpParm(result, thissop, "detectverts", cookparms.getCookTime(), 0);
        return result;
    }
    bool getOutputsegs() const { return myOutputsegs; }
    void setOutputsegs(bool val) { myOutputsegs = val; }
    bool opOutputsegs(const SOP_NodeVerb::CookParms &cookparms) const
    { 
        SOP_Node *thissop = cookparms.getNode();
        if (!thissop) return getOutputsegs();
        bool result;
        OP_Utils::evalOpParm(result, thissop, "outputsegs", cookparms.getCookTime(), 0);
        return result;
    }
    bool getUseinputnumattrib() const { return myUseinputnumattrib; }
    void setUseinputnumattrib(bool val) { myUseinputnumattrib = val; }
    bool opUseinputnumattrib(const SOP_NodeVerb::CookParms &cookparms) const
    { 
        SOP_Node *thissop = cookparms.getNode();
        if (!thissop) return getUseinputnumattrib();
        bool result;
        OP_Utils::evalOpParm(result, thissop, "useinputnumattrib", cookparms.getCookTime(), 0);
        return result;
    }
    const UT_StringHolder & getInputnumattrib() const { return myInputnumattrib; }
    void setInputnumattrib(const UT_StringHolder & val) { myInputnumattrib = val; }
    UT_StringHolder opInputnumattrib(const SOP_NodeVerb::CookParms &cookparms) const
    { 
        SOP_Node *thissop = cookparms.getNode();
        if (!thissop) return getInputnumattrib();
        UT_StringHolder result;
        OP_Utils::evalOpParm(result, thissop, "inputnumattrib", cookparms.getCookTime(), 0);
        return result;
    }
    bool getUseprimnumattrib() const { return myUseprimnumattrib; }
    void setUseprimnumattrib(bool val) { myUseprimnumattrib = val; }
    bool opUseprimnumattrib(const SOP_NodeVerb::CookParms &cookparms) const
    { 
        SOP_Node *thissop = cookparms.getNode();
        if (!thissop) return getUseprimnumattrib();
        bool result;
        OP_Utils::evalOpParm(result, thissop, "useprimnumattrib", cookparms.getCookTime(), 0);
        return result;
    }
    const UT_StringHolder & getPrimnumattrib() const { return myPrimnumattrib; }
    void setPrimnumattrib(const UT_StringHolder & val) { myPrimnumattrib = val; }
    UT_StringHolder opPrimnumattrib(const SOP_NodeVerb::CookParms &cookparms) const
    { 
        SOP_Node *thissop = cookparms.getNode();
        if (!thissop) return getPrimnumattrib();
        UT_StringHolder result;
        OP_Utils::evalOpParm(result, thissop, "primnumattrib", cookparms.getCookTime(), 0);
        return result;
    }
    bool getUseprimuvwattrib() const { return myUseprimuvwattrib; }
    void setUseprimuvwattrib(bool val) { myUseprimuvwattrib = val; }
    bool opUseprimuvwattrib(const SOP_NodeVerb::CookParms &cookparms) const
    { 
        SOP_Node *thissop = cookparms.getNode();
        if (!thissop) return getUseprimuvwattrib();
        bool result;
        OP_Utils::evalOpParm(result, thissop, "useprimuvwattrib", cookparms.getCookTime(), 0);
        return result;
    }
    const UT_StringHolder & getPrimuvwattrib() const { return myPrimuvwattrib; }
    void setPrimuvwattrib(const UT_StringHolder & val) { myPrimuvwattrib = val; }
    UT_StringHolder opPrimuvwattrib(const SOP_NodeVerb::CookParms &cookparms) const
    { 
        SOP_Node *thissop = cookparms.getNode();
        if (!thissop) return getPrimuvwattrib();
        UT_StringHolder result;
        OP_Utils::evalOpParm(result, thissop, "primuvwattrib", cookparms.getCookTime(), 0);
        return result;
    }
    bool getUseptnumattrib() const { return myUseptnumattrib; }
    void setUseptnumattrib(bool val) { myUseptnumattrib = val; }
    bool opUseptnumattrib(const SOP_NodeVerb::CookParms &cookparms) const
    { 
        SOP_Node *thissop = cookparms.getNode();
        if (!thissop) return getUseptnumattrib();
        bool result;
        OP_Utils::evalOpParm(result, thissop, "useptnumattrib", cookparms.getCookTime(), 0);
        return result;
    }
    const UT_StringHolder & getPtnumattrib() const { return myPtnumattrib; }
    void setPtnumattrib(const UT_StringHolder & val) { myPtnumattrib = val; }
    UT_StringHolder opPtnumattrib(const SOP_NodeVerb::CookParms &cookparms) const
    { 
        SOP_Node *thissop = cookparms.getNode();
        if (!thissop) return getPtnumattrib();
        UT_StringHolder result;
        OP_Utils::evalOpParm(result, thissop, "ptnumattrib", cookparms.getCookTime(), 0);
        return result;
    }

private:
    UT_StringHolder myAgroup;
    UT_StringHolder myBgroup;
    bool myUseproxtol;
    fpreal64 myProxtol;
    bool myDetectverts;
    bool myOutputsegs;
    bool myUseinputnumattrib;
    UT_StringHolder myInputnumattrib;
    bool myUseprimnumattrib;
    UT_StringHolder myPrimnumattrib;
    bool myUseprimuvwattrib;
    UT_StringHolder myPrimuvwattrib;
    bool myUseptnumattrib;
    UT_StringHolder myPtnumattrib;

};
